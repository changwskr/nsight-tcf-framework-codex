# PDMK 표 아키텍처 정의서

| 항목 | 내용 |
| ---- | ---- |
| 목적 | 통합 아키텍처 **전체 범위**를 **표 + TEXT 표 그림**만으로 정의 |
| 대상 | `pdmk-fw` · `pdmk-service` · `pdmk-ui` |
| 범위 | 원본 §1~§29 · Part I~VIII **전 주제** (절별 표·표 그림) |
| 기준일 | 2026-08-08 |
| 읽기 정본 | [PDMK_아키텍처_정의서.md](./PDMK_아키텍처_정의서.md) (v2.0) |
| 입문 요약 | [아키텍처 정의서.md](./아키텍처%20정의서.md) (왜→검증) |
| 서술 상세 | [통합 아키텍처 정의서 copy.md](./통합%20아키텍처%20정의서%20copy.md) |
| 관련 | [네이밍원칙.md](./네이밍원칙.md) · [MK-NAMING_CONVENTION.md](./MK-NAMING_CONVENTION.md) |

> 본 문서는 **표·표 그림 요약본**이다 (정본 아님).  
> **읽기 정본:** [PDMK_아키텍처_정의서.md](./PDMK_아키텍처_정의서.md).  
> 장문 서술·체크리스트는 copy를 본다.

---

## 목차

### 문서 맵(표 그림)

```text
 ┌─────────────────────────────────────────────────────────────┐
 │                    PDMK 통합 아키텍처 정의서                  │
 └───────────────────────────┬─────────────────────────────────┘
                             │
     ┌───────────┬───────────┼───────────┬───────────┐
     ▼           ▼           ▼           ▼           ▼
  I 개요     II 런타임   III 구조    IV 계약     V 데이터
  §1~§3      §4~§6       §7~§9      §10~§14    §15~§17
     │           │           │           │           │
     └───────────┴───────────┼───────────┴───────────┘
                             │
               ┌─────────────┼─────────────┐
               ▼             ▼             ▼
          VI 횡단      VII 플랫폼     VIII 지침
         §18~§22       §23~§25       §26~§29
```

| 부 | 원본 절 | 본문 |
| -- | ------- | ---- |
| I 개요 | §1~§3 | [T1](#t1-개요-원본-13) |
| II 런타임 | §4~§6 | [T2](#t2-런타임-원본-46) |
| III 구조 | §7~§9 | [T3](#t3-구조모듈-원본-79) |
| IV 계약 | §10~§14 | [T4](#t4-계약-원본-1014) |
| V 데이터 | §15~§17 | [T5](#t5-데이터-원본-1517) |
| VI 횡단 | §18~§22 | [T6](#t6-횡단-원본-1822) |
| VII 플랫폼 | §23~§25 | [T7](#t7-플랫폼-원본-2325) |
| VIII 지침 | §26~§29 | [T8](#t8-지침실무-원본-2629) |

| 슬로건 | 내용 |
| ------ | ---- |
| 1 | 세 모듈 한 스택 |
| 2 | 서비스 ID = URL = 메서드 |
| 3 | commons ON · TCF OFF |
| 4 | TX는 Service |
| 5 | 전문은 hdr+dto |
| 6 | 변경은 영향 표로 (T8) |


### 전체 소절 목차 (전부 TEXT 표 그림 포함)

| 소절 | 원본 |
| ---- | ---- |
| **T1. 개요 (원본 §1~§3)** | |
| [T1.1 스택 계층(표 그림)](#t11-스택-계층표-그림-11) | §1.1 |
| [T1.2 런타임 호출 체인](#t12-런타임-호출-체인-12) | §1.2 |
| [T1.3 관점 맵](#t13-관점-맵-13) | §1.3 |
| [T1.4 모듈 × 관심사](#t14-모듈-관심사-14) | §1.4 |
| [T1.5 빌드·기동 빅픽처](#t15-빌드기동-빅픽처-15) | §1.5 |
| [T1.6 문서 구성(부)](#t16-문서-구성부-16) | §1.6 |
| [T1.7 목적·3모듈](#t17-목적3모듈-21) | §2.1 |
| [T1.8 절↔모듈 매핑](#t18-절모듈-매핑-22) | §2.2 |
| [T1.9 범위 안/밖(표 그림)](#t19-범위-안밖표-그림-23) | §2.3 |
| [T1.10 배포 토폴로지(표 그림)](#t110-배포-토폴로지표-그림-31) | §3.1 |
| [T1.11 모듈 계약(표 그림)](#t111-모듈-계약표-그림-32) | §3.2 |
| [T1.12 책임 한줄](#t112-책임-한줄-33) | §3.3 |
| **T2. 런타임 (원본 §4~§6)** | |
| [T2.1 UI 경유 통합 경로(표 그림)](#t21-ui-경유-통합-경로표-그림-40) | §4.0 |
| [T2.2 service+fw 계층(표 그림)](#t22-servicefw-계층표-그림-41) | §4.1 |
| [T2.3 성공 경로 타임라인(표 그림)](#t23-성공-경로-타임라인표-그림-42) | §4.2 |
| [T2.4 전체 시퀀스(표 그림)](#t24-전체-시퀀스표-그림-51) | §5.1 |
| [T2.5 삼층 역할(표 그림)](#t25-삼층-역할표-그림-5) | §5 |
| [T2.6 비교 표](#t26-비교-표-52) | §5.2 |
| [T2.7 Filter 단계별 책임(표 그림)](#t27-filter-단계별-책임표-그림-53) | §5.3 |
| [T2.8 Interceptor 생명주기(표 그림)](#t28-interceptor-생명주기표-그림-54) | §5.4 |
| [T2.9 BizAspect(표 그림)](#t29-bizaspect표-그림-55) | §5.5 |
| [T2.10 운영 로그 타임라인](#t210-운영-로그-타임라인-56) | §5.6 |
| [T2.11 삼층 스위치](#t211-삼층-스위치-57) | §5.7 |
| [T2.12 필터 맵(표 그림)](#t212-필터-맵표-그림-61) | §6.1 |
| [T2.13 Filter 등록](#t213-filter-등록-62) | §6.2 |
| [T2.14 Filter 처리 흐름(표 그림)](#t214-filter-처리-흐름표-그림-63) | §6.3 |
| [T2.15 Body 캐시(표 그림)](#t215-body-캐시표-그림-64) | §6.4 |
| [T2.16 JWT 게이트(표 그림)](#t216-jwt-게이트표-그림-65) | §6.5 |
| [T2.17 Context·MDC 수명(표 그림)](#t217-contextmdc-수명표-그림-66) | §6.6 |
| [T2.18 체인 위치·후속](#t218-체인-위치후속-67) | §6.7 |
| [T2.19 Security 충돌 주의](#t219-security-충돌-주의-68) | §6.8 |
| [T2.20 Filter 스위치·체크](#t220-filter-스위치체크-69610) | §6.9·6.10 |
| **T3. 구조·모듈 (원본 §7~§9)** | |
| [T3.1 전체 레이어(표 그림)](#t31-전체-레이어표-그림-71) | §7.1 |
| [T3.2 service 업무 레이어(표 그림)](#t32-service-업무-레이어표-그림-72) | §7.2 |
| [T3.3 fw 레이어 위치](#t33-fw-레이어-위치-73) | §7.3 |
| [T3.4 ui 레이어](#t34-ui-레이어-74) | §7.4 |
| [T3.5 PDMK vs 패키지 리팩터형](#t35-pdmk-vs-패키지-리팩터형-75) | §7.5 |
| [T3.6 레이어 체크](#t36-레이어-체크-76) | §7.6 |
| [T3.7 모듈 배치(표 그림)](#t37-모듈-배치표-그림-8) | §8 |
| [T3.8 fw 패키지](#t38-fw-패키지-81) | §8.1 |
| [T3.9 service 구성](#t39-service-구성-82) | §8.2 |
| [T3.10 ui 중계(표 그림)](#t310-ui-중계표-그림-83) | §8.3 |
| [T3.11 commons 위치·역할](#t311-commons-위치역할-91) | §9.1 |
| [T3.12 commons 패키지 맵](#t312-commons-패키지-맵-92) | §9.2 |
| [T3.13 commons 파이프라인(표 그림)](#t313-commons-파이프라인표-그림-93) | §9.3 |
| [T3.14 컨텍스트·헤더·에러](#t314-컨텍스트헤더에러-94) | §9.4 |
| [T3.15 감사·로그·연동](#t315-감사로그연동-95) | §9.5 |
| [T3.16 util·TX 경계](#t316-utiltx-경계-96) | §9.6 |
| [T3.17 commons vs TCF(표 그림)](#t317-commons-vs-tcf표-그림-97) | §9.7 |
| [T3.18 commons 체크](#t318-commons-체크-98) | §9.8 |
| **T4. 계약 (원본 §10~§14)** | |
| [T4.1 전문 전체 구조(표 그림)](#t41-전문-전체-구조표-그림-101) | §10.1 |
| [T4.2 요청 전문·local](#t42-요청-전문local-102) | §10.2 |
| [T4.3 sys_comm 필드](#t43-syscomm-필드-103) | §10.3 |
| [T4.4 업무 dto(표 그림)](#t44-업무-dto표-그림-104) | §10.4 |
| [T4.5 요청·응답 바인딩(표 그림)](#t45-요청응답-바인딩표-그림-105) | §10.5 |
| [T4.6 서비스ID·전문 연결](#t46-서비스id전문-연결-106) | §10.6 |
| [T4.7 전문 하지 말 것](#t47-전문-하지-말-것-107) | §10.7 |
| [T4.8 트랜잭션 두 의미](#t48-트랜잭션-두-의미-11) | §11 |
| [T4.9 트랜잭션 구조(표 그림)](#t49-트랜잭션-구조표-그림-111) | §11.1 |
| [T4.10 DB TX 인프라](#t410-db-tx-인프라-112) | §11.2 |
| [T4.11 권장 선언](#t411-권장-선언-113) | §11.3 |
| [T4.12 샘플 TX 갭](#t412-샘플-tx-갭-114) | §11.4 |
| [T4.13 업무 vs DB TX 시퀀스(표 그림)](#t413-업무-vs-db-tx-시퀀스표-그림-115) | §11.5 |
| [T4.14 ImageLog·TX 분리(표 그림)](#t414-imagelogtx-분리표-그림-116) | §11.6 |
| [T4.15 ttl_ug_ync](#t415-ttlugync-117) | §11.7 |
| [T4.16 TX 체크](#t416-tx-체크-118) | §11.8 |
| [T4.17 에러 경로(표 그림)](#t417-에러-경로표-그림-121) | §12.1 |
| [T4.18 예외 타입](#t418-예외-타입-122) | §12.2 |
| [T4.19 메시지 사전(표 그림)](#t419-메시지-사전표-그림-123) | §12.3 |
| [T4.20 commons 에러 조립](#t420-commons-에러-조립-124) | §12.4 |
| [T4.21 Filter 인증 오류](#t421-filter-인증-오류-125) | §12.5 |
| [T4.22 업무 사용 권장](#t422-업무-사용-권장-126) | §12.6 |
| [T4.23 이미지로그와 에러](#t423-이미지로그와-에러-127) | §12.7 |
| [T4.24 TCF 에러 경로](#t424-tcf-에러-경로-128) | §12.8 |
| [T4.25 에러 체크](#t425-에러-체크-129) | §12.9 |
| [T4.26 예외 계층(표 그림)](#t426-예외-계층표-그림-131) | §13.1 |
| [T4.27 예외 전파(표 그림)](#t427-예외-전파표-그림-132) | §13.2 |
| [T4.28 포착 지점](#t428-포착-지점-133) | §13.3 |
| [T4.29 업무 예외 시퀀스(표 그림)](#t429-업무-예외-시퀀스표-그림-134) | §13.4 |
| [T4.30 예외 vs 결과코드](#t430-예외-vs-결과코드-135) | §13.5 |
| [T4.31 NhExceptionProvider](#t431-nhexceptionprovider-136) | §13.6 |
| [T4.32 Controller throws](#t432-controller-throws-137) | §13.7 |
| [T4.33 TCF 예외·체크](#t433-tcf-예외체크-138139) | §13.8·13.9 |
| [T4.34 네이밍(표 그림)](#t434-네이밍표-그림-14) | §14 |
| [T4.35 네이밍 하지 말 것·체크](#t435-네이밍-하지-말-것체크-145146) | §14.5·14.6 |
| **T5. 데이터 (원본 §15~§17)** | |
| [T5.0 Part 맵(표 그림)](#t50-part-맵표-그림) |  |
| [T5.1 DAO 위치·구성(표 그림)](#t51-dao-위치구성표-그림-151) | §15.1 |
| [T5.2 메서드·SQL ID](#t52-메서드sql-id-152) | §15.2 |
| [T5.3 샘플 DAO](#t53-샘플-dao-153) | §15.3 |
| [T5.4 Mapper XML 계약](#t54-mapper-xml-계약-154) | §15.4 |
| [T5.5 인프라 Bean(표 그림)](#t55-인프라-bean표-그림-155) | §15.5 |
| [T5.6 Service↔DAO 책임(표 그림)](#t56-servicedao-책임표-그림-156) | §15.6 |
| [T5.7 DAO 금지·체크](#t57-dao-금지체크-157158) | §15.7·15.8 |
| [T5.8 테이블 DS·위치(표 그림)](#t58-테이블-ds위치표-그림-161) | §16.1 |
| [T5.9 테이블 소유(표 그림)](#t59-테이블-소유표-그림-162) | §16.2 |
| [T5.10 명명·소유](#t510-명명소유-163) | §16.3 |
| [T5.11 IMAGE_LOG 컬럼](#t511-imagelog-컬럼-164) | §16.4 |
| [T5.12 SALES_TIP 컬럼](#t512-salestip-컬럼-165) | §16.5 |
| [T5.13 TB_MK_CO_A_5530](#t513-tbmkcoa5530-166) | §16.6 |
| [T5.14 메시지 번들·매트릭스](#t514-메시지-번들매트릭스-167168) | §16.7·16.8 |
| [T5.15 환경·경계](#t515-환경경계-1691610) | §16.9·16.10 |
| [T5.16 페이징(표 그림)](#t516-페이징표-그림-17) | §17 |
| **T6. 횡단 (원본 §18~§22)** | |
| [T6.1 보안(표 그림)](#t61-보안표-그림-18) | §18 |
| [T6.2 Security 구성·갭](#t62-security-구성갭-182188) | §18.2·18.8 |
| [T6.3 CORS·헤더](#t63-cors헤더-18) | §18 |
| [T6.4 AOP(표 그림)](#t64-aop표-그림-19) | §19 |
| [T6.5 AOP 경계](#t65-aop-경계-19) | §19 |
| [T6.6 타임아웃(표 그림)](#t66-타임아웃표-그림-20) | §20 |
| [T6.7 로그(표 그림)](#t67-로그표-그림-21) | §21 |
| [T6.8 캐시(표 그림)](#t68-캐시표-그림-22) | §22 |
| **T7. 플랫폼 (원본 §23~§25)** | |
| [T7.1 Boot·스캔](#t71-boot스캔-231) | §23.1 |
| [T7.2 설정 적층(표 그림)](#t72-설정-적층표-그림-232) | §23.2 |
| [T7.3 프로파일](#t73-프로파일-23) | §23 |
| [T7.4 설정 키](#t74-설정-키-239) | §23.9 |
| [T7.5 Gradle(표 그림)](#t75-gradle표-그림-24) | §24 |
| [T7.6 Boot 스타터 핀](#t76-boot-스타터-핀-242) | §24.2 |
| [T7.7 환경·기동(표 그림)](#t77-환경기동표-그림-25) | §25 |
| [T7.8 컨테이너](#t78-컨테이너-25) | §25 |
| **T8. 지침·실무 (원본 §26~§29)** | |
| [T8.1 갭(표 그림)](#t81-갭표-그림-260) | §26.0 |
| [T8.2 Do / Don't](#t82-do-dont-261262) | §26.1~26.2 |
| [T8.3 TCF 스위치(표 그림)](#t83-tcf-스위치표-그림-263) | §26.3 |
| [T8.4 새 거래(표 그림)](#t84-새-거래표-그림-264) | §26.4 |
| [T8.5 참고 문서](#t85-참고-문서-27) | §27 |
| [T8.6 용어·색인](#t86-용어색인-28) | §28 |
| [T8.7 샘플 거래 맵](#t87-샘플-거래-맵-284) | §28.4 |
| [T8.8 설정 키→절](#t88-설정-키절-285) | §28.5 |
| [T8.9 역할별 읽기](#t89-역할별-읽기-291) | §29.1 |
| [T8.10 환경 매트릭스](#t810-환경-매트릭스-292) | §29.2 |
| [T8.11 장애(표 그림)](#t811-장애표-그림-293) | §29.3 |
| [T8.12 변경 영향](#t812-변경-영향-294) | §29.4 |
| [T8.13 포트(표 그림)](#t813-포트표-그림-295) | §29.5 |

> 위 모든 소절·부 헤더 본문에 TEXT(`text` 코드블록) 표 그림이 있다.
---

## T1. 개요 (원본 §1~§3)

```text
  §1 빅픽처 ──► 스택·호출체인·관점맵
       │
  §2 범위   ──► 3모듈 정의 · 범위 밖(TCF/PDMG)
       │
  §3 구성   ──► 포트·의존·책임 한줄
       │
       └──────► [Part II 런타임]
```

### T1.1 스택 계층(표 그림) — §1.1

```text
 ┌──────────────────────────────────────────┐
 │  Presentation   pdmk-ui :8090            │
 │  정적화면 · 카탈로그 · HTTP 릴레이         │
 └────────────────────┬─────────────────────┘
                      │ POST /{서비스ID}
 ┌────────────────────▼─────────────────────┐
 │  Application    pdmk-service :8080       │
 │  Controller → Service → DAO · BizAspect  │
 └────────────────────┬─────────────────────┘
                      │ implementation
 ┌────────────────────▼─────────────────────┐
 │  Framework      pdmk-fw (commons) JAR    │
 │  Filter · Interceptor · JWT · ImageLog   │
 └────────────────────┬─────────────────────┘
                      │
 ┌────────────────────▼─────────────────────┐
 │  Persistence    RDW (H2 / Oracle)        │
 │  TB_* · MyBatis · DS rdw                 │
 └──────────────────────────────────────────┘
```

| 모듈 | 산출 | 포트 | 역할 |
| ---- | ---- | ---- | ---- |
| `pdmk-ui` | Boot JAR | 8090 | 전문 편집·중계 |
| `pdmk-service` | WAR(+fw) | 8080 | 업무 API |
| `pdmk-fw` | JAR | — | commons 파이프라인 |

### T1.2 런타임 호출 체인 — §1.2

```text
 Browser / UI
   → DefaultFilter (fw)
   → SecurityFilterChain (service)
   → ServicePreventionInterceptor (fw)
   → BizPrePostAspect (service)
   → Controller → Service → DAO (service)
   → Response Advice / ImageLog (fw)
```

| 순 | 계층 | 모듈 |
| -- | ---- | ---- |
| 1 | UI Relay | ui |
| 2 | DefaultFilter | fw |
| 3 | SecurityFilterChain | service |
| 4 | ServicePreventionInterceptor | fw |
| 5 | BizPrePostAspect | service |
| 6 | Controller→Service→DAO | service |
| 7 | Response Advice / ImageLog | fw |

### T1.3 관점 맵 — §1.3

```text
 [런타임] T2=§4~6     [구조] T3=§7~9
 [계약]   T4=§10~14   [데이터] T5=§15~17
 [횡단]   T6=§18~22   [플랫폼] T7=§23~25
 [지침]   T8=§26~29
```

| 관점 | 본문 | 원본 |
| ---- | ---- | ---- |
| 런타임 경로 | T2 | §4~§6 |
| 레이어·모듈 | T3 | §7~§9 |
| 전문·TX·에러·네이밍 | T4 | §10~§14 |
| DAO·테이블·페이징 | T5 | §15~§17 |
| 보안·AOP·타임아웃·로그·캐시 | T6 | §18~§22 |
| Spring·Gradle·배포 | T7 | §23~§25 |
| 경계·실무 | T8 | §26~§29 |

### T1.4 모듈 × 관심사 — §1.4

```text
          pdmk-fw   pdmk-service   pdmk-ui
 헤더/Filter   ●          ○            ○
 업무 C/S/DAO  —          ●            —
 BizAspect     —          ●            —
 ImageLog      ●          ○            —
 릴레이/카탈로그 —         —            ●
 MyBatis/TX    ○          ●            —
 ※ ●주 ○부 —없음  (copy §1.4)
```

| 관심사 | fw | service | ui |
| ------ | -- | ------- | -- |
| 전문 헤더 | ● | ○ | ○ |
| 업무 DTO/C/S/DAO | — | ● | — |
| Filter·JWT·Context | ● | — | — |
| Interceptor·ImageLog | ● | — | — |
| BizAspect | — | ● | — |
| Security 샘플 | ○ | ● | — |
| 릴레이·카탈로그 | — | — | ● |
| MyBatis·테이블 | ○ | ● | — |

### T1.5 빌드·기동 빅픽처 — §1.5

```text
 [1] pdmk-fw        → java-library JAR
 [2] pdmk-service   → implementation(fw) → WAR · :8080
 [3] pdmk-ui        → Boot JAR · :8090  (fw 미의존)
 Wrapper Gradle · JDK 21 · Spring Boot 3.5.x
```

### T1.6 문서 구성(부) — §1.6

```text
 ┌─────────────────────────────────────────────────────────────┐
 │                    PDMK 통합 아키텍처 정의서                  │
 └───────────────────────────┬─────────────────────────────────┘
                             │
     ┌───────────┬───────────┼───────────┬───────────┐
     ▼           ▼           ▼           ▼           ▼
  I 개요     II 런타임   III 구조    IV 계약     V 데이터
  §1~§3      §4~§6       §7~§9      §10~§14    §15~§17
     │           │           │           │           │
     └───────────┴───────────┼───────────┴───────────┘
                             │
               ┌─────────────┼─────────────┐
               ▼             ▼             ▼
          VI 횡단      VII 플랫폼     VIII 지침
         §18~§22       §23~§25       §26~§29
```

| Part | 절 | 본문 |
| ---- | -- | ---- |
| I | §1~3 | T1 |
| II | §4~6 | T2 |
| III | §7~9 | T3 |
| IV | §10~14 | T4 |
| V | §15~17 | T5 |
| VI | §18~22 | T6 |
| VII | §23~25 | T7 |
| VIII | §26~29 | T8 |

### T1.7 목적·3모듈 — §2.1

```text
                 ┌──────────── pdmk-ui ────────────┐
 Browser ──────► │ static + /api/relay             │
                 └───────────────┬─────────────────┘
                                 │ HTTP POST /{서비스ID}
                 ┌───────────────▼─────────────────┐
                 │         pdmk-service            │
                 │  nhnis.mk.*  Controller…DAO     │
                 └───────────────┬─────────────────┘
                                 │ implementation
                 ┌───────────────▼─────────────────┐
                 │           pdmk-fw               │
                 │  nhnis.fw.commons.* (+ tcf 비활성)│
                 └───────────────┬─────────────────┘
                                 ▼
                              RDW DB
```

| 모듈 | 한줄 |
| ---- | ---- |
| fw | 공통 런타임(commons) |
| service | 업무 거래·DAO |
| ui | 전문 테스트·중계 |

### T1.8 절↔모듈 매핑 — §2.2

```text
 §4~6, §9, §10~13, §18~22  →  fw (+ service)
 §7~8, §11, §14~17, §26.4 →  service
 §4.0, §8.3, §20, §25     →  ui
 §23~25                  →  전 모듈
```

| 원본 절 묶음 | 주 모듈 |
| ------------ | ------- |
| §4~6, §9, §10~13, §18~22 | fw(+service) |
| §7~8, §11, §14~17, §26.4 | service |
| §4.0, §8.3, §20, §25 | ui |
| §23~25 | 전 모듈 |

### T1.9 범위 안/밖(표 그림) — §2.3

```text
 ┌────────────── PDMK 정의서 범위 ──────────────┐
 │  pdmk-ui · pdmk-service · pdmk-fw(commons)   │
 │  서비스ID · hdr+dto · RDW 샘플               │
 └──────────────────────┬───────────────────────┘
                        │ 범위 밖
           ┌────────────┴────────────┐
           ▼                         ▼
    nhnis.fw.tcf.* (잔존)      PDMG / PDMP 등
    tcf.enabled=false          별도 스택
```

### T1.10 배포 토폴로지(표 그림) — §3.1

```text
┌─────────────────┐     HTTP relay      ┌──────────────────┐
│    pdmk-ui      │ ──────────────────► │  pdmk-service    │
│  :8090          │   POST /mkcoa*      │  :8080           │
│  static + API   │                     │  nhnis.mk.*      │
└────────┬────────┘                     └────────┬─────────┘
         │                                       │
         │ 브라우저                               │ implementation
         ▼                                       ▼
   /mkcoa5530 등                          ┌──────────────┐
   /imagelog                              │   pdmk-fw    │
                                          │ nhnis.fw.*   │
                                          └──────┬───────┘
                                                 │
                                                 ▼
                                          RDW (H2/Oracle)
                                          MyBatis XML
```

### T1.11 모듈 계약(표 그림) — §3.2

```text
  pdmk-ui                 pdmk-service              pdmk-fw
 ┌─────────┐   HTTP만   ┌─────────────┐  Gradle  ┌─────────┐
 │ :8090   │ ─────────► │ :8080 WAR   │ ◄─────── │ JAR     │
 │ 릴레이  │            │ nhnis.mk.*  │ project  │commons  │
 └─────────┘            └─────────────┘          └─────────┘
      │                        │                      │
      └──────── 서비스ID · hdr_nhnis+dto 계약 ────────┘
      ※ ui ↔ fw 직접 의존 없음
```

| 계약 | 내용 |
| ---- | ---- |
| HTTP | `POST /{서비스ID}` · `application/json` |
| 전문 | `hdr_nhnis` + `dto` |
| UI 중계 | `/api/relay/{서비스ID}` → 동일 본문 |

### T1.12 책임 한줄 — §3.3

```text
 pdmk-fw      : 컨텍스트·헤더·에러·ImageLog·Filter
 pdmk-service : 업무·TX·DAO·Security 샘플
 pdmk-ui      : 편집·릴레이·카탈로그
 ※ ui ↔ fw 직접 의존 없음 (copy §3.2)
```

| 모듈 | 책임 |
| ---- | ---- |
| fw | 컨텍스트·헤더·에러·ImageLog·Filter |
| service | 업무·TX·DAO·Security 샘플 |
| ui | 편집·릴레이·카탈로그 |

---

## T2. 런타임 (원본 §4~§6)

```text
 UI ──HTTP──► §4 요청흐름
                │
                ▼
         §5 Filter / Interceptor / Aspect 역할
                │
                ▼
         §6 DefaultFilter 상세 (Body·JWT·Context)
                │
                └──────► Controller (Part III~)
```

### T2.1 UI 경유 통합 경로(표 그림) — §4.0

```text
 Browser
   → pdmk-ui  (static 또는 POST /api/relay/{서비스ID})
        → TransactionRelayService
             → pdmk-service  POST /{서비스ID}
                  → [아래 §4.1 fw + service 계층]
```

### T2.2 service+fw 계층(표 그림) — §4.1

```text
 HTTP POST /{서비스ID}
 Body: { "hdr_nhnis": {...}, "dto": {...} }
       (local: { "dto": {...} } 만으로도 가능)
────────────────────────────────────────────────────────────
 │ ① DefaultFilter                 [Servlet Filter · pdmk-fw]
 │    Body 캐시 · JWT · ServiceContext · MDC
 ▼
 │ ② ServicePreventionInterceptor  [HandlerInterceptor · pdmk-fw]
 │    ┌ 선처리(preHandle)  GUID·헤더보강 · ImageLog PRE
 │    └ 후처리(postHandle) ImageLog POST
 │    └ 예외(afterCompletion) ImageLog EX · Context 제거
 ▼
 │ ③ BizPrePostAspect              [AOP · pdmk-service]
 │    ┌ 선처리(@Before)  업무 공통 로그 · BRC
 │    └ 후처리(@After)   업무 공통 로그 · BRC
 ▼
 │ ④ Controller → ⑤ Service → ⑥ DAO / MyBatis XML   [pdmk-service]
────────────────────────────────────────────────────────────
 응답: *DTOout JSON  →  (ui 경유 시) RelayResult 로 브라우저에 전달
```

### T2.3 성공 경로 타임라인(표 그림) — §4.2

```text
 t0  UI relay POST
 t1  DefaultFilter      Body · JWT · Context
 t2  Security           permitAll
 t3  Interceptor        GUID · ImageLog PRE
 t4  BizAspect @Before  업무선처리 로그
 t5  Controller → Service → DAO
 t6  BizAspect @After   업무후처리 로그
 t7  Interceptor        ImageLog POST
 t8  Filter finally     Context / MDC clear
 t9  UI ← DTOout
```

| 순 | 단계 | 모듈 | 위치 |
| -- | ---- | ---- | ---- |
| 0 | UI 중계 | ui | `TransactionRelayService` |
| 1 | Filter | fw | `DefaultFilter.doFilter` |
| 2 | 시스템 선처리 | fw | Interceptor.preHandle |
| 3 | 업무 선처리 | service | Aspect.before |
| 4 | 업무 실행 | service | C→S→DAO |
| 5 | 업무 후처리 | service | Aspect.after |
| 6 | 시스템 후처리 | fw | Interceptor.postHandle |
| 7 | Filter finally | fw | Context·MDC 정리 |

### T2.4 전체 시퀀스(표 그림) — §5.1

```text
 Client                Filter              Interceptor           Aspect              Controller/Service
   │                     │                     │                   │                      │
   │── POST /mkcoa* ────►│                     │                   │                      │
   │                     │ Body 캐시/파싱       │                   │                      │
   │                     │ JWT(비-local)        │                   │                      │
   │                     │ ServiceContext set   │                   │                      │
   │                     │──────── doFilter ───►│                   │                      │
   │                     │                     │ preHandle         │                      │
   │                     │                     │ GUID·헤더보강      │                      │
   │                     │                     │ ImageLog PRE      │                      │
   │                     │                     │──────────────────►│                      │
   │                     │                     │                   │ @Before              │
   │                     │                     │                   │ 업무선처리 로그       │
   │                     │                     │                   │─────────────────────►│
   │                     │                     │                   │                      │ 업무 로직
   │                     │                     │                   │◄─────────────────────│
   │                     │                     │                   │ @After               │
   │                     │                     │                   │ 업무후처리 로그       │
   │                     │                     │◄──────────────────│                      │
   │                     │                     │ postHandle        │                      │
   │                     │                     │ ImageLog POST     │                      │
   │                     │◄────────────────────│                   │                      │
   │                     │ finally: Context/MDC clear              │                      │
   │◄── *DTOout ─────────│                     │                   │                      │
```

### T2.5 삼층 역할(표 그림) — §5

```text
 ┌─ DefaultFilter ──────────┐  Servlet
 │ Body · JWT · Context     │
 └────────────┬─────────────┘
              ▼
 ┌─ ServicePreventionInterceptor ┐  MVC
 │ GUID · 헤더 · ImageLog        │
 └────────────┬──────────────────┘
              ▼
 ┌─ BizPrePostAspect ───────┐  Spring AOP
 │ 업무 선·후처리 로그        │
 └────────────┬─────────────┘
              ▼
         Controller
```

| 구분 | 클래스 | 기술 | 스위치 | 주 책임 |
| ---- | ------ | ---- | ------ | ------- |
| 필터 | `DefaultFilter` | Servlet Filter | `commons.filter` | 파싱·JWT·Context |
| 시스템 | `ServicePreventionInterceptor` | HandlerInterceptor | `legacy-web` | GUID·ImageLog |
| 업무 | `BizPrePostAspect` | Spring AOP | `legacy-web` | Controller 로그 |

### T2.6 비교 표 — §5.2

```text
 ┌──────────────┬─────────────────────┬──────────────────┐
 │ DefaultFilter│ ServicePrevention…  │ BizPrePostAspect │
 │ Servlet      │ MVC Interceptor     │ Spring AOP       │
 │ Body·JWT·Ctx │ GUID·ImageLog       │ 업무 선후 로그    │
 │ 401/400 stop │ EX 로그             │ 예외 전파        │
 └──────────────┴─────────────────────┴──────────────────┘
            copy §5.2 비교 요지
```

| 항목 | Filter | Interceptor | Aspect |
| ---- | ------ | ----------- | ------ |
| 계층 | Servlet | MVC | AOP around Controller |
| URL | 전역 | `/**` (`/error` 제외) | `nhnis.mk.co..controller..*` |
| Body | 읽기·캐시 | 안 읽음 | 안 읽음 |
| JWT | 비-local | — | — |
| GUID | 설정 | 없으면 채번 | — |
| ImageLog | — | PRE/POST/EX | — |
| 로그 | 자체 | `PdmkTxLog.system*` | `PdmkTxLog.biz*` |
| 실패 | 401/400 sendError | EX 로그 | 예외 전파 |

### T2.7 Filter 단계별 책임(표 그림) — §5.3

```text
                    ┌─────────────────────────────┐
                    │      DefaultFilter          │
                    └──────────────┬──────────────┘
                                   ▼
              content-type = multipart/* ?
                    ┌──────────────┴──────────────┐
                   Yes                           No
                    ▼                             ▼
           X-GUID 헤더로 Context          CachedBody 래핑
           구성 후 통과                   JWT 검사(비-local)
                                          Body 비어있음? → 400
                                          JSON 파싱 실패? → 400
                                          hdr_nhnis 없음?
                                            · local → 합성 헤더
                                            · 그 외 → 400
                                          MDC(guid/ip/user/service)
                                          ServiceContextHolder.set
                                          filterChain.doFilter(wrapper)
                                   │
                                   ▼
                              finally
                     Context remove + ThreadContext clear
```

| 단계 | 동작 | 결과 |
| ---- | ---- | ---- |
| URI | path 마지막 세그먼트 | MDC serviceId |
| multipart | X-GUID 최소 헤더 | Body 미파싱 |
| JWT 비-local | Bearer Access | 실패 401 |
| Body | CachedBody | 빈 Body → 400 |
| hdr_nhnis | Map→DTO | local 합성 / 그외 400 |
| 보강 | rms_svc_c, scid, IP, optr_eno… | 비어 있을 때만 |
| finally | Context·MDC 제거 | 누수 방지 |

| 조건 | local | 비-local |
| ---- | ----- | -------- |
| JWT | 생략 | Bearer 필수 |
| hdr 없음 | 합성 허용 | 400 |
| optr_eno 빈값 | `"LOCAL"` | 동일 보강 |

### T2.8 Interceptor 생명주기(표 그림) — §5.4

```text
 preHandle                          postHandle                    afterCompletion
 ─────────                          ──────────                    ───────────────
 SystemPreProcessor Start!          SystemPostProcessor           (예외 있을 때만)
 Context null? → warn 후 통과       multipart? skip               SystemErrorProcessor
 GUID 없으면 UUID 강제 채번         ImageLog POST                 ImageLog EX
 헤더 보강(서비스/화면/IP/사용자)                                  Context remove + rethrow
 ImageLog PRE
```

| 메서드 | 시점 | 처리 |
| ------ | ---- | ---- |
| preHandle | C 전 | GUID·enrich·preImagelog |
| postHandle | 정상 후 | postImagelog |
| afterCompletion | 예외 | exceptionImagelog·rethrow |

| 필드 | 비어 있을 때 |
| ---- | ------------ |
| rms_svc_c | URI 마지막 세그먼트 |
| scid | rms_svc_c에서 `[SD]\d+$` 제거 |
| tr_trm_ipadr | XFF 또는 remoteAddr |
| optr_eno | MDC userId 또는 `"LOCAL"` |

### T2.9 BizAspect(표 그림) — §5.5

```text
          Controller 메서드 호출
                   │
                   ▼
         ┌─────────────────────┐
         │ @Before  업무 선처리 │  ▶▶▶▶▶▶ mpco 업무 공통 선처리 ▶▶▶▶▶▶
         │ Argument BRC 로그   │  getTrtBrc / getBrc / getBRC / getL5104
         └──────────┬──────────┘
                    ▼
              Controller body
              Service / DAO
                    │
                    ▼
         ┌─────────────────────┐
         │ @After   업무 후처리 │  ▶▶▶▶▶▶ mpco 업무 공통 후처리 ▶▶▶▶▶▶
         │ Argument BRC 로그   │
         └─────────────────────┘
```

| 항목 | 값 |
| ---- | -- |
| Pointcut | `nhnis.mk.co..controller..*` |
| Order | 100 |
| 활성 | legacy-web=true |
| 비담당 | DB·ImageLog·TX |

### T2.10 운영 로그 타임라인 — §5.6

```text
[ServicePreventionInterceptor] SystemPreProcessor Start!
[ServicePreventionInterceptor] GUID: <std_gbl_id>
▶▶▶▶▶▶ mpco 업무 공통 선처리 ▶▶▶▶▶▶
[bizPrePostAspect().before()] Argument: BRC : ...
▷▷▷▷▷▷▷▷ mkcoa5530 Controller Start!
▶▶▶▶▶▶▶▶ mkcoa5530S0 Service Start!
▶▶▶▶▶▶▶▶ mkcoa5530S0 Service End! - Total: n
▷▷▷▷▷▷▷▷ mkcoa5530 Controller End!...
▶▶▶▶▶▶ mpco 업무 공통 후처리 ▶▶▶▶▶▶
[bizPrePostAspect().after()] Argument: BRC : ...
[ServicePreventionInterceptor] SystemPostProcessor
```

### T2.11 삼층 스위치 — §5.7

```text
 nhnis.fw.commons.filter.enabled=true     → DefaultFilter
 nhnis.fw.commons.legacy-web.enabled=true → Interceptor·Resolver·BizAspect
 spring.profiles.active=local             → JWT 생략·hdr 합성
 nhnis.fw.tcf.enabled=false               → TCF 경로 미사용
```

| 키 | 영향 |
| -- | ---- |
| commons.filter.enabled | DefaultFilter |
| commons.legacy-web.enabled | Interceptor·Resolver·BizAspect |
| profiles=local | JWT 생략·헤더 합성 |
| tcf.enabled=false | TCF 미사용 |

### T2.12 필터 맵(표 그림) — §6.1

```text
 ┌─────────────────────── Servlet Container ───────────────────────┐
 │  FilterRegistrationBean  order=1  urlPatterns=/*                 │
 │                                                                  │
 │  ┌──────────────── DefaultFilter (ACTIVE) ─────────────────┐   │
 │  │  multipart? / JWT? / Body cache / hdr_nhnis / Context    │   │
 │  └──────────────────────────┬──────────────────────────────┘   │
 │                             ▼                                    │
 │  Spring Security FilterChain (앱 SecurityConfig)                 │
 │  └─ 샘플: permitAll (상태비저장)                                  │
 │                             ▼                                    │
 │  DispatcherServlet                                               │
 │  └─ Interceptor → Controller …                                  │
 └──────────────────────────────────────────────────────────────────┘

  (비활성) TcfTraceFilter / JwtAuthenticationFilter  — tcf.enabled=true 일 때만
```

| 필터 | 모듈 | 활성 | 역할 |
| ---- | ---- | ---- | ---- |
| DefaultFilter | fw | filter.enabled | 전문·JWT·Context |
| TcfTraceFilter | fw tcf | tcf.enabled | 비활성 |
| JwtAuthenticationFilter | fw tcf | TCF Security | 비활성·병행금지 |
| SecurityFilterChain | service | 항상 | permitAll 등 |

### T2.13 Filter 등록 — §6.2

```text
FilterConfiguration  (@ConditionalOnProperty filter.enabled=true)
  └─ FilterRegistrationBean<DefaultFilter>
        setFilter(defaultFilter)
        addUrlPatterns("/*")
        setOrder(1)
```

### T2.14 Filter 처리 흐름(표 그림) — §6.3

```text
                    DefaultFilter.doFilter
                              │
                              ├─ URI → serviceId → MDC
                              ├─ HTTP 헤더 수집 (HttpHeaders)
                              │
              content-type starts with multipart/ ?
                     ┌────────┴────────┐
                    Yes               No
                     │                 │
                     ▼                 ▼
              X-GUID 헤더로      CachedBodyHttpServletRequest
              최소 hdr_nhnis           │
              Context set        비-local? JWT Bearer 검증
              doFilter(원본)           │ 실패 → 401 return
                     │           Body 비어있음? → 400
                     │           JSON 파싱 실패? → 400
                     │           hdr_nhnis 없음?
                     │             local → syntheticLocalHeader
                     │             else → 400
                     │           sys_comm 보강
                     │           MDC(guid/ip/userId/serviceId)
                     │           ServiceContextHolder.set
                     │           doFilter(wrapper)  ← Body 재읽기 가능
                     │                 │
                     └────────┬────────┘
                              ▼
                           finally
                     Context remove + ThreadContext.clearAll
```

### T2.15 Body 캐시(표 그림) — §6.4

```text
 원본 Request
      │ readAllBytes()
      ▼
 CachedBodyHttpServletRequest
      ├─ getInputStream() / getReader() → 캐시 재재생
      └─ Filter 이후 RequestBodyArgumentResolver 가 동일 Body 재파싱
```

### T2.16 JWT 게이트(표 그림) — §6.5

```text
 spring.profiles.active
         │
    local │                    그 외 │
         ▼                         ▼
   JWT 검증 생략              Authorization: Bearer
   (Filter 통과)              JwtProvider.validate
                              ├─ OK → ssoId attribute
                              └─ NG → 401 sendError
```

### T2.17 Context·MDC 수명(표 그림) — §6.6

```text
 Filter 성공
    │ set ServiceContextHolder + MDC(guid…)
    ▼
  … Interceptor / Aspect / Controller …
    │
 Filter finally
    │ remove Context + ThreadContext.clearAll
    ▼
 워커 재사용 안전 (GUID 누수 방지)
```

| 저장소 | 내용 |
| ------ | ---- |
| ServiceContextHolder | appName, guid, headers, hdr_nhnis… |
| ThreadContext(MDC) | guid, ip, userId, serviceId |

### T2.18 체인 위치·후속 — §6.7

```text
 DefaultFilter          ← 전문·Context 준비 (본 절)
      │
 SecurityFilterChain    ← 인가(샘플 permitAll)
      │
 DispatcherServlet
      │
 ServicePreventionInterceptor  ← 시스템 선후·ImageLog  [§5.4]
      │
 BizPrePostAspect              ← 업무 로그           [§5.5]
      │
 Controller → Service → DAO
```

### T2.19 Security 충돌 주의 — §6.8

```text
 ❌ commons.security.enabled + 앱 SecurityConfig 이중 ON
 ❌ TCF JwtAuthenticationFilter + DefaultFilter JWT 병행
 ✅ tcf.enabled=false · commons.security=false · 앱 Security만
```

| 금지 | 이유 |
| ---- | ---- |
| fw commons Security + 앱 Security 이중 ON | 체인 충돌 |
| TCF JWT Filter + DefaultFilter JWT 병행 | 이중 검증 |

### T2.20 Filter 스위치·체크 — §6.9·6.10

```text
 ┌─ nhnis.fw.commons.* ─────────────────────┐
 │ filter.enabled=true ★                     │
 │ legacy-web.enabled=true ★                 │
 │ security.enabled=false ★                  │
 │ tcf.enabled=false ★                       │
 └───────────────────────────────────────────┘
 체크: finally Context/MDC clear · TCF Filter OFF
```

| 키 | 기본 | 의미 |
| -- | ---- | ---- |
| commons.filter.enabled | true | DefaultFilter |
| commons.legacy-web.enabled | true | Interceptor·Resolver·Aspect |
| tcf.enabled | false | TCF OFF |
| commons.security.enabled | false | fw Security 미사용 |

| 체크 |
| ---- |
| filter.enabled=true |
| local이면 JWT 스킵 확인 |
| finally clear 누락 없음 |
| TCF Filter 비활성 |

---

## T3. 구조·모듈 (원본 §7~§9)

```text
  ┌──────── §7 레이어드 ────────┐
  │ Presentation → Application  │
  │ → Domain → Persistence      │
  └──────────────┬──────────────┘
                 │
     ┌───────────┼───────────┐
     ▼           ▼           ▼
  §8.1 fw     §8.2 service  §8.3 ui
     │
     └──────► §9 commons 패키지 맵
```

### T3.1 전체 레이어(표 그림) — §7.1

```text
┌─────────────────────────────────────────────────────────────────┐
│                        Presentation / Entry                      │
│  pdmk-ui: static HTML · PdmkUiApiController · HomeController     │
│  pdmk-service: nhnis.mk.co.a.controller.*                        │
└───────────────────────────────┬─────────────────────────────────┘
                                │ HTTP / 메서드 호출
┌───────────────────────────────▼─────────────────────────────────┐
│                     Cross-cutting (횡단)                         │
│  pdmk-fw: DefaultFilter · ServicePreventionInterceptor           │
│           RequestBody / ResponseBody Resolver · JwtProvider      │
│  pdmk-service: BizPrePostAspect · SecurityConfig · WebMvc        │
└───────────────────────────────┬─────────────────────────────────┘
                                │
┌───────────────────────────────▼─────────────────────────────────┐
│                     Application / Service                        │
│  nhnis.mk.co.a.service.*     (메서드명 = 서비스 ID)               │
│  nhnis.mk.co.a.dto.*         (DTOin / DTOout / DTOSub)           │
│  nhnis.mk.common.util.*      (MappingUtil 등)                    │
└───────────────────────────────┬─────────────────────────────────┘
                                │
┌───────────────────────────────▼─────────────────────────────────┐
│                     Persistence                                  │
│  nhnis.mk.co.a.dao.*  (@RDWMapper)                               │
│  resources/rdw.mk.co.a/*-ORA.xml                                 │
└───────────────────────────────┬─────────────────────────────────┘
                                │ JDBC
┌───────────────────────────────▼─────────────────────────────────┐
│                     Infrastructure                               │
│  RDW DataSource (H2 local / Oracle) · MyBatis · HikariCP         │
└─────────────────────────────────────────────────────────────────┘
```

### T3.2 service 업무 레이어(표 그림) — §7.2

```text
  Client
    │  POST /mkcoa5530S0   { hdr_nhnis, dto }
    ▼
┌───────────────┐
│  Controller   │  nhnis.mk.co.a.controller
│  진입·위임만  │  @PostMapping("/서비스ID")
└───────┬───────┘
        │ DTOin → DTOout
        ▼
┌───────────────┐
│   Service     │  nhnis.mk.co.a.service
│  업무 규칙    │  페이징·매핑·검증
└───────┬───────┘
        │ Map / int
        ▼
┌───────────────┐
│     DAO       │  nhnis.mk.co.a.dao
│  SQL ID 호출  │  메서드명 = SQL ID
└───────┬───────┘
        │ MyBatis
        ▼
┌───────────────┐
│  Mapper XML   │  rdw.mk.co.a/{식별}-ORA.xml
└───────┬───────┘
        ▼
       RDW
```

| 레이어 | 허용 | 금지 |
| ------ | ---- | ---- |
| Controller | 매핑·위임 | SQL·TX·비즈니스 |
| Service | 규칙·TX·페이징·Map↔DTO | HTTP |
| DAO | SQL ID만 | DTO·비즈니스 |

### T3.3 fw 레이어 위치 — §7.3

```text
                    ┌──────────────────────┐
                    │   pdmk-service 업무   │
                    │ Controller/Service/  │
                    │ DAO/DTO              │
                    └──────────┬───────────┘
                               │ uses
         ┌─────────────────────▼─────────────────────┐
         │              pdmk-fw                       │
         │  commons.filter / interceptor / resolver   │
         │  commons.dto.header / jwt / imagelog / util│
         │  (tcf.* — 비활성)                          │
         └───────────────────────────────────────────┘
```

| 관심 | 패키지 |
| ---- | ------ |
| Filter | commons.filter |
| Interceptor | commons.interceptor |
| Resolver | commons.resolver |
| header DTO | commons.dto.header |
| 예외 | commons.exception |
| ImageLog | commons.imagelog |
| TCF(OFF) | nhnis.fw.tcf.* |

### T3.4 ui 레이어 — §7.4

```text
 Browser (static HTML/JS)
         │
         ▼
 ┌───────────────────┐
 │ entry.web         │  Home · /api/* Controller
 └─────────┬─────────┘
           ▼
 ┌───────────────────┐
 │ application       │  TransactionCatalog
 └─────────┬─────────┘
           ▼
 ┌───────────────────┐
 │ client            │  TransactionRelayService → pdmk-service
 └───────────────────┘
           +
 ┌───────────────────┐
 │ support / config  │  RelayResult · Properties
 └───────────────────┘
```

### T3.5 PDMK vs 패키지 리팩터형 — §7.5

```text
 [PDMK 현재 · copy §7.5]
  서비스ID 단위 Controller/Service/DAO · Map DAO
 [참고 리팩터형]
  domain / application 분리 · Entity Repository
 ※ 본 스택은 서비스ID 축 유지
```

| PDMK(현재) | 참고(리팩터형) |
| ---------- | -------------- |
| 서비스ID 단위 C/S/DAO | domain/application 분리형 |
| Map DAO | 엔티티 Repository |

### T3.6 레이어 체크 — §7.6

```text
 ✅ Controller → Service → DAO 단방향
 ✅ @Transactional = Service만
 ✅ DTO = nhnis.mk.*.dto · DAO 입출력=Map
 ❌ Controller→DAO · DAO에 DTOin/out
```

| 체크 |
| ---- |
| C→S→DAO 단방향 |
| TX=Service |
| DTO는 service.dto |
| fw는 업무 테이블 SQL 직접 소유 최소화(ImageLog 예외) |

### T3.7 모듈 배치(표 그림) — §8

```text
 ┌─ §8.1 pdmk-fw ────┐
 │ java-library JAR  │◄── implementation
 │ nhnis.fw.commons  │         │
 └───────────────────┘         │
 ┌─ §8.2 pdmk-service ┐        │
 │ Boot WAR :8080     │────────┘
 │ nhnis.mk.*         │◄── HTTP POST
 └────────────────────┘         │
 ┌─ §8.3 pdmk-ui ─────┐         │
 │ Boot JAR :8090     │─────────┘
 │ 릴레이 · 카탈로그   │  (fw 미의존)
 └────────────────────┘
```

### T3.8 fw 패키지 — §8.1

```text
 pdmk-fw (JAR, bootJar off)
  ├─ nhnis.fw.commons.*  ★ 사용 (filter/interceptor/resolver/…)
  └─ nhnis.fw.tcf.*        소스 잔존 · tcf.enabled=false
```

| 영역 | 내용 |
| ---- | ---- |
| commons | Filter·Interceptor·Resolver·header·exception·imagelog |
| tcf | 소스 잔존 · enabled=false |
| 산출 | jar · bootJar off |

### T3.9 service 구성 — §8.2

```text
nhnis.mk
├── PdmkApplication / ServletInitializer
├── config/                 # Security, MyBatis(RDW), CORS, WebMvc
├── common/util/            # MappingUtil 등
└── co/
    ├── common/             # BizPrePostAspect (업무 선·후처리)
    └── a/                  # 업무 CO + 세부 A
        ├── controller/
        ├── service/
        ├── dao/
        └── dto/
```

| 영역 | 내용 |
| ---- | ---- |
| 업무 | nhnis.mk.co.a.* |
| 설정 | Security·RdwDataSource·RDWMapper |
| 리소스 | rdw.mk.co.a/*-ORA.xml · db/h2 |
| 산출 | war |

### T3.10 ui 중계(표 그림) — §8.3

```text
nhnis.mk.ui
├── PdmkUiApplication
├── config/PdmkUiProperties     # target-base-url, timeout
├── entry/web/
│   ├── PdmkUiHomeController    # 정적 화면 라우팅
│   └── PdmkUiApiController     # /api/*
├── application/service/
│   └── TransactionCatalog      # 등록 거래·샘플 JSON
├── client/
│   └── TransactionRelayService # RestClient 중계
└── support/                    # RelayResult, TransactionInfo
```

### T3.11 commons 위치·역할 — §9.1

```text
 nhnis.fw.commons
 ├─ configuration / filter / interceptor / resolver   ← 요청 파이프라인
 ├─ context / jwt / imagelog / log                    ← 컨텍스트·감사
 ├─ dto / exception / message                         ← 전문·에러
 ├─ transaction                                       ← (레거시 TX 홀더, 업무 TX는 앱)
 ├─ apigw / fos                                       ← 외부 연동 유틸
 └─ util                                              ← 문자열·날짜·JSON 등
```

| 항목 | 내용 |
| ---- | ---- |
| 패키지 루트 | `nhnis.fw.commons` |
| 역할 | 요청 파이프라인·헤더·에러·감사 |
| 활성 | filter + legacy-web |

### T3.12 commons 패키지 맵 — §9.2

```text
 nhnis.fw.commons
  ├─ filter / interceptor / resolver
  ├─ dto.header (hdr_nhnis, sys_comm)
  ├─ exception / context / imagelog
  ├─ message (MessageLoader/Cache 옵션)
  └─ util · transaction 경계
```

| 패키지 | 역할 |
| ------ | ---- |
| filter | DefaultFilter · CachedBody |
| interceptor | ServicePrevention · ImageLog |
| resolver | Request/Response Body |
| dto.header | hdr_nhnis · sys_comm |
| exception | NhBaseException · Provider |
| context | ServiceContextHolder |
| imagelog | ImageLogHandler |
| message | MessageLoader/Cache(옵션) |
| util / transaction | 유틸·경계 |

### T3.13 commons 파이프라인(표 그림) — §9.3

```text
 DefaultFilter          filter + jwt + context
      │
 Security (앱)          ← commons SecurityConfig 와 이중 금지
      │
 ServicePreventionInterceptor   interceptor + imagelog
      │
 RequestBodyArgumentResolver    resolver (dto)
      │
 Controller (service)
      │
 ResponseBodyArgumentResolver   예외 시 NH_NIS_ERR_DTO
```

### T3.14 컨텍스트·헤더·에러 — §9.4

```text
 DefaultFilter
   → ServiceContextHolder + hdr_nhnis
 ResponseBodyArgumentResolver
   → 정상: hdr+dto | 예외: NhBaseException→NH_NIS_ERR_DTO
```

| 축 | 컴포넌트 |
| -- | -------- |
| Context | ServiceContextHolder |
| 헤더 | hdr_nhnis / sys_comm |
| 에러 | NhBaseException → NH_NIS_ERR_DTO |

### T3.15 감사·로그·연동 — §9.5

```text
 ImageLogHandler PRE/POST/EX → TB_FW_IMAGE_LOG
 PdmkTxLog (system*/biz*)     → Slf4j
 MessageCache (옵션)          → framework.message.*
```

| 축 | 컴포넌트 |
| -- | -------- |
| 감사 | ImageLog PRE/POST/EX |
| 로그 | PdmkTxLog (service 측 포맷 사용) |
| 메시지 | MessageCache 옵션 |

### T3.16 util·TX 경계 — §9.6

```text
 ┌─ 업무 DB TX ────────────────┐  ┌─ ImageLog JDBC ────────┐
 │ Service @Transactional      │  │ Interceptor 경로       │
 │ rdwTransactionManager       │  │ 업무 TX와 커밋 분리 가능│
 └─────────────────────────────┘  └─ 실패 삼킴 ────────────┘
```

| 규칙 | 내용 |
| ---- | ---- |
| DB TX | service `@Transactional` + rdwTM |
| fw | ImageLog JDBC는 업무 TX와 분리 가능 |

### T3.17 commons vs TCF(표 그림) — §9.7

```text
                 pdmk-fw
        ┌──────────┴──────────┐
        ▼                     ▼
 nhnis.fw.commons.*     nhnis.fw.tcf.*
  filter / interceptor    Aspect / STF·ETF
  hdr_nhnis + dto         StandardRequestDto
  NhBaseException         BizException
        │                     │
        │ enabled=true        │ enabled=false (PDMK)
        ▼                     ▼
   ★ PDMK 런타임          (소스만 잔존)
```

| | commons | TCF |
| - | ------- | --- |
| 스위치 | filter/legacy-web | tcf.enabled |
| PDMK | **사용** | **미사용** |

### T3.18 commons 체크 — §9.8

```text
 ☐ tcf.enabled=false
 ☐ filter.enabled + legacy-web.enabled = true
 ☐ commons.security.enabled=false (앱과 이중 금지)
```

| 체크 |
| ---- |
| tcf.enabled=false |
| filter+legacy-web=true |
| 앱 Security와 fw Security 이중 OFF/단일 |

---

## T4. 계약 (원본 §10~§14)

```text
  서비스 ID (§14)
       │
       ▼
  ┌─ 전문 hdr+dto (§10) ─┐
  │         │              │
  │    ┌────┴────┐         │
  │    ▼         ▼         │
  │  TX(§11)  에러/예외    │
  │           (§12·§13)    │
  └────────────────────────┘
```

### T4.1 전문 전체 구조(표 그림) — §10.1

```text
┌──────────────────────── 요청 / 응답 JSON ────────────────────────┐
│                                                                  │
│  hdr_nhnis                          ← 공통 헤더 (FW)              │
│  └─ sys_comm                        ← 시스템 공통 필드            │
│       ├─ std_gbl_id                 GUID                          │
│       ├─ rms_svc_c                  서비스 ID (= URL)             │
│       ├─ scid                       화면/프로그램 ID              │
│       ├─ tr_trm_ipadr / tr_brc …    단말·점·조작자 등             │
│       └─ …                                                        │
│                                                                  │
│  dto                                ← 업무 본문 (서비스별)         │
│  ├─ [요청] *DTOin 필드 (pageNo, guidList …)                       │
│  └─ [응답] *DTOout 필드                                           │
│         ├─ *DTOSub0[]               목록 행(있을 때)              │
│         ├─ size / pageNo / …        페이징·건수                   │
│         └─ …                                                      │
└──────────────────────────────────────────────────────────────────┘
```

| 노드 | Java 타입 | 소유 |
| ---- | --------- | ---- |
| hdr_nhnis | commons.dto.header.hdr_nhnis | fw |
| sys_comm | commons.dto.header.sys_comm | fw |
| dto 요청 | `{서비스ID}DTOin` | service |
| dto 응답 | `{서비스ID}DTOout` | service |
| 에러 | NH_NIS_ERR_DTO | fw |

| HTTP | 규칙 |
| ---- | ---- |
| Method/URL | `POST /{서비스ID}` |
| Content-Type | application/json |
| 클래스 @RequestMapping | 사용 안 함 |

### T4.2 요청 전문·local — §10.2

| 프로파일 | hdr_nhnis | Filter |
| -------- | --------- | ------ |
| local | 선택 | 없으면 합성 |
| 비-local | **필수** | 없으면 400 + JWT |

```text
 [비-local] { "hdr_nhnis": {"sys_comm":{…}}, "dto":{…} }  필수+JWT
 [local]    { "dto": {…} } 만 가능 → Filter syntheticLocalHeader
```

### T4.3 sys_comm 필드 — §10.3

```text
 hdr_nhnis.sys_comm
  ★ std_gbl_id · rms_svc_c · scid · tr_trm_ipadr · optr_eno
  · sync_dsc · tr_sysid · ttl_ug_ync · tr_dtm · tr_brc …
  (copy §10.3 — ImageLog/MDC 핵심 5필드 ★)
```

| 필드 | 의미 | 쓰임 |
| ---- | ---- | ---- |
| std_gbl_id | GUID | MDC·ImageLog 키 |
| rms_svc_c | 서비스 ID | URL·카탈로그 일치 |
| scid | 화면/프로그램 ID | 보통 식별까지 |
| orgtr_gbl_id / trz_gbl_id | 원·부가 GUID | 선택 |
| sync_dsc | 동기 구분 | 샘플 S |
| tr_sysid | 시스템 ID | PDMK 등 |
| ttl_ug_ync | TTL 사용 | 샘플 0 · TX 미연동 |
| std_tgrm_rqr_rsp_dsc | 요구/응답 | Q |
| std_tgrm_lclc | 로케일 | KO |
| tr_trm_ipadr | 단말 IP | 로그·MDC |
| tr_dtm | 일시 | YYYYMMDDHHMMSS |
| tr_brc / trmno / trm_kdc | 점·단말 | |
| optr_eno / tr_optrnm | 조작자 | userId |

**핵심:** std_gbl_id · rms_svc_c · scid · tr_trm_ipadr · optr_eno

### T4.4 업무 dto(표 그림) — §10.4

```text
서비스 ID  mkcoa5530S0
           │
           ├─ mkcoa5530S0DTOin     ← 요청 dto
           ├─ mkcoa5530S0DTOout    ← 응답 dto
           └─ mkcoa5530S0DTOSub0   ← 목록 1행 (있을 때)
```

| 구분 | 패턴 | 상속 |
| ---- | ---- | ---- |
| 입력 | `{ID}DTOin` | DataObject |
| 출력 | `{ID}DTOout` | DataObject |
| 목록행 | `{ID}DTOSub{n}` | DataObject |

| 서비스 | 요청 주요 | 응답 주요 |
| ------ | --------- | --------- |
| mkcoa5530S0 | trtBrc,basDt,pageNo,pageSize | Sub0[],size,페이징 |
| mkcoa9999S0 | 조건(페이징❌) | Sub0[],size |
| mkcoa8888S0 | 페이징 | ImageLog Sub |
| mkcoa8888D0 | guidList[] | 삭제 결과 |

```text
 DTOout
  ├─ *DTOSub0[]
  ├─ size / pageNo / pageSize
  └─ totalCount / totalPages
```

### T4.5 요청·응답 바인딩(표 그림) — §10.5

```text
 [요청]
 JSON Body
    │
    ├─ DefaultFilter ──────────► hdr_nhnis → ServiceContext
    │
    └─ RequestBodyArgumentResolver
           root.get("dto") → {서비스ID}DTOin
           (@nhnis.fw.commons.resolver.RequestBody)

 [응답]
 Controller return {서비스ID}DTOout
    │
    └─ ResponseBodyArgumentResolver (ResponseBodyAdvice)
           {
             "hdr_nhnis": ServiceContext.header,
             "dto": DTOout
           }
           ※ dtoLogicalName, fieldPropertyMap, *List/*Array 필드 제거
```

| 단계 | 컴포넌트 | 처리 |
| ---- | -------- | ---- |
| 요청 헤더 | DefaultFilter | hdr→Context |
| 요청 본문 | RequestBodyArgumentResolver | **dto만** |
| 응답 | ResponseBodyArgumentResolver | 헤더+dto |
| 어노테이션 | `@nhnis.fw.commons.resolver.RequestBody` | Spring 표준 아님 |

### T4.6 서비스ID·전문 연결 — §10.6

```text
[대구분2][업무2][세부1][식별4][구분자1][순번1]
   mk      co     a     8888     S       0
→ mkcoa8888S0
```

| 위치 | 값 예 |
| ---- | ----- |
| URL | POST /mkcoa8888S0 |
| rms_svc_c | mkcoa8888S0 |
| scid | mkcoa8888 |
| 메서드 | mkcoa8888S0(...) |
| DTO | *DTOin/out |
| SQL ID | mkcoa8888S0_S0 |

### T4.7 전문 하지 말 것 — §10.7

```text
 ❌ 루트에 업무필드 (hdr+dto만 인식)
 ❌ Spring 표준 @RequestBody (dto 추출 실패)
 ❌ DTOout만 응답 끝 (Advice가 envelope 조립)
 ❌ TCF StandardDto / result.resultCode 혼용
 ❌ REST /api/… 업무 URL
```

| 금지 | 이유 |
| ---- | ---- |
| 루트에 업무 필드 | hdr+dto만 인식 |
| Spring @RequestBody | dto 추출 실패 |
| DTOout만 응답 끝 | Advice가 envelope 조립 |
| TCF StandardDto 혼용 | 계약 상이 |
| REST 자원 URL로 서비스ID 대체 | 네이밍 붕괴 |

### T4.8 트랜잭션 두 의미 — §11

```text
 ┌─ 업무 거래 ──────────────────────────┐
 │ POST 1건: Filter→Interceptor→Aspect→C/S │
 └──────────────────┬───────────────────┘
                    │ 포함 가능
 ┌─ DB TX ──────────▼───────────────────┐
 │ rdwTransactionManager + @Transactional │
 └──────────────────────────────────────┘
 (OFF) TCF @TcfTransaction — DB commit 아님
```

| 구분 | 의미 | PDMK |
| ---- | ---- | ---- |
| 업무 거래 | POST 1건 생명주기 | Filter→…→C/S |
| DB TX | commit/rollback | rdwTM + @Transactional |
| TCF @TcfTransaction | 업무 AOP | **미사용** |

### T4.9 트랜잭션 구조(표 그림) — §11.1

```text
 ┌────────────────── 업무 거래 (요청 1건) ──────────────────┐
 │  DefaultFilter → Interceptor → BizAspect → Controller   │
 │         │                                                │
 │         ▼                                                │
 │  ┌──────────── Service (권장: @Transactional) ────────┐ │
 │  │  Spring TX Proxy                                    │ │
 │  │       │                                             │ │
 │  │       ▼                                             │ │
 │  │  rdwTransactionManager (DataSourceTransactionManager)│ │
 │  │       │                                             │ │
 │  │       ▼                                             │ │
 │  │  rdwDataSource → SqlSessionTemplate → DAO SQL       │ │
 │  │       │                                             │ │
 │  │  정상 → commit(쓰기) / 종료(읽기)                    │ │
 │  │  예외 → rollback                                    │ │
 │  └─────────────────────────────────────────────────────┘ │
 │  Interceptor ImageLog PRE/POST  (별도 SQL, 업무 TX와 분리)│
 └──────────────────────────────────────────────────────────┘
```

### T4.10 DB TX 인프라 — §11.2

```text
RdwDataSourceConfig
├─ rdwDataSource          (@Primary, HikariCP)
├─ rdwSqlSessionFactory   (MyBatis + rdw.*/*.xml)
├─ rdwSqlSessionTemplate
└─ rdwTransactionManager  (@Primary, DataSourceTransactionManager)
         │
         ▼
   동일 DataSource의 Connection으로 SQL 묶음
```

| Bean | 역할 |
| ---- | ---- |
| rdwDataSource | @Primary Hikari |
| rdwTransactionManager | @Primary |
| SqlSessionTemplate | 동일 DS → TX 합류 |

### T4.11 권장 선언 — §11.3

```text
@Service
@Transactional(readOnly = true)          ← 클래스 기본: 조회
public class mkcoaXXXXService {

    public ...S0(...) { ... }             ← 읽기 TX

    @Transactional(timeout = 4)           ← 메서드가 클래스 설정을 덮어씀
    public ...D0(...) { ... }             ← 쓰기 TX (commit/rollback)
}
```

| 용도 | 애노테이션 |
| ---- | ---------- |
| 조회 | @Transactional(readOnly=true) |
| 쓰기 | @Transactional (± timeout 초) |
| 위치 | **Service만** |

### T4.12 샘플 TX 갭 — §11.4

```text
 [copy §11.4] 일부 Service @Transactional 미명시 = 갭
 [권장] DB 접근 Service 전부 명시 (조회 readOnly / 쓰기)
```

| 상태 | 내용 |
| ---- | ---- |
| 일부 Service | @Transactional 미명시(갭) |
| 권장 | 모든 DB 접근 Service에 명시 |

### T4.13 업무 vs DB TX 시퀀스(표 그림) — §11.5

```text
 Client
   │ POST /mkcoa8888D0
   ▼
 Filter / Interceptor(pre) / Aspect(before)
   │
   ▼
 Controller
   │
   ▼
 ┌─ Spring @Transactional (쓰기) ─┐
 │  Service.mkcoa8888D0           │
 │    → DAO.delete                │
 │  성공: commit                  │
 │  RuntimeException 등: rollback │
 └────────────────────────────────┘
   │
   ▼
 Aspect(after) / Interceptor(post) / Filter finally
   │
   ▼
 Response { hdr_nhnis, dto }
```

### T4.14 ImageLog·TX 분리(표 그림) — §11.6

```text
 Interceptor PRE ── ImageLog INSERT ── (별도 커밋 가능)
        │
        ▼
 ┌─ @Transactional Service ─┐
 │  DAO SELECT / DML         │
 │  commit / rollback        │
 └────────────┬──────────────┘
              ▼
 Interceptor POST/EX ── ImageLog UPDATE ── (업무 TX와 분리)

 ※ 업무 rollback 후에도 PRE/EX 감사행이 남을 수 있음
```

### T4.15 ttl_ug_ync — §11.7

```text
 sys_comm.ttl_ug_ync  ··· ✕자동연동✕ ···  @Transactional(timeout 초)
      전문 플래그(샘플 0)              Spring DB TX timeout
```

| 필드 | 연동 |
| ---- | ---- |
| sys_comm.ttl_ug_ync | @Transactional(timeout)과 **자동 연동 없음** |

### T4.16 TX 체크 — §11.8

```text
 ☐ TX=Service · C/DAO에 @Transactional 없음
 ☐ ImageLog 실패 ≠ 업무 강제 롤백
 ☐ Template·TM 동일 rdwDataSource
```

| 체크 |
| ---- |
| TX=Service |
| C/DAO에 @Transactional 없음 |
| ImageLog 실패 ≠ 업무 롤백 강제 |
| rdwTM = Primary |

### T4.17 에러 경로(표 그림) — §12.1

```text
                    예외·오류 발생 지점
                           │
     ┌─────────────────────┼─────────────────────┐
     ▼                     ▼                     ▼
 Filter 단계            Controller 전후        Service / DAO
 (JSON·JWT·Header)      (MVC / Advice)         (업무 로직)
     │                     │                     │
     │ sendError(4xx)      │                     │ throw NhBaseException
     │                     │                     │   (권장) / RuntimeException
     ▼                     ▼                     ▼
 HTTP 상태+문구        ResponseBodyArgumentResolver
 (전문 envelope 없음)   @ExceptionHandler(NhBaseException)
                              │
                              ▼
                        NH_NIS_ERR_DTO 조립
                        메시지 해석 (yml / MessageCache)
                              │
                              ▼
                        ResponseBodyAdvice
                        { hdr_nhnis, dto: 에러DTO }
```

### T4.18 예외 타입 — §12.2

```text
 ★ NhBaseException (commons) — PDMK 권장
 ✕ BizException (TCF) — tcf.enabled=false 미사용
 ✕ 날것 NPE 등 — 비권장
```

| 타입 | 용도 |
| ---- | ---- |
| NhBaseException | **권장** |
| BizException(TCF) | PDMK 미사용 |
| NPE 등 | 비권장 |

### T4.19 메시지 사전(표 그림) — §12.3

```text
 exceptionCode.yml          MessageCache
 nhnis.exception.FWxxxx  →  (메모리 Map)
         │                        │
         ▼                        ▼
 Spring property          MessageLoader 등이 put
 (앱이 classpath import)
         │                        │
         └────────┬───────────────┘
                  ▼
     errorProcessor / ETF(비활성) 가 MessageFormat 적용
```

### T4.20 commons 에러 조립 — §12.4

```text
 NhBaseException
   ├─ stdErrCode
   ├─ errMsgType
   ├─ messageValue[]          ← MessageFormat 인자
   ├─ stdErrMsgContents       ← 이미 완성된 문장이면 그대로
   └─ 클래스/메서드/라인/스택
         │
         ▼
 NH_NIS_ERR_DTO
   ├─ stdErrCode
   ├─ stdErrMsgCntn           ← 해석된 메시지
   ├─ addMsgContents
   ├─ errClassName / errFileName / errMethodName / errLineNo
   ├─ errType
   └─ stackTrace (상위 15줄)
         │
         ▼
 ResponseEntity 500 + body
         │
         ▼
 beforeBodyWrite → { "hdr_nhnis": ..., "dto": NH_NIS_ERR_DTO }
```

| 단계 | 동작 |
| ---- | ---- |
| 코드조회 | yml / MessageCache |
| 포맷 | MessageFormat 인자 |
| 응답 dto | NH_NIS_ERR_DTO |

### T4.21 Filter 인증 오류 — §12.5

```text
 Filter 실패 ──► response.sendError(status, "한글 문구")
                  (hdr_nhnis/dto 조립 안 함)
```

| 단계 | 응답 |
| ---- | ---- |
| Filter 실패 | 401/400 sendError · **envelope 없음** |

### T4.22 업무 사용 권장 — §12.6

```text
 Service
   │ 검증 실패 / 대상 없음 / 중복
   ▼
 throw new NhBaseException("FW0001", new String[]{"요청 Body"}, TYPE.COMMON);
   또는
 throw new NhBaseException("MP0404", TYPE.BIZ);  // MessageCache
   │
   ▼
 @Transactional rollback (선언 시)
   │
   ▼
 errorProcessor → NH_NIS_ERR_DTO → { hdr_nhnis, dto }
```

| 상황 | 권장 |
| ---- | ---- |
| 검증·시스템 | throw NhBaseException(code) |
| 업무 0건 등 | DTOout 결과코드 |

### T4.23 이미지로그와 에러 — §12.7

```text
 업무 예외 → Interceptor.afterCompletion → exceptionImagelog
 ImageLog JDBC 실패 → 삼킴 (업무 예외로 미전파)  [copy §12.7·§11.6]
```

| 항목 | 내용 |
| ---- | ---- |
| EX | afterCompletion exceptionImagelog |
| ImageLog JDBC 실패 | 업무 예외로 미전파(삼킴) |

### T4.24 TCF 에러 경로 — §12.8

```text
 tcf.enabled=false → TCF 에러/BizException 경로 미사용 (참고만)
```

| 항목 | 내용 |
| ---- | ---- |
| TCF | enabled=false · 참고만 |

### T4.25 에러 체크 — §12.9

```text
 ☐ NhBaseException + exceptionCode.yml
 ☐ Filter 401/400 sendError ≠ MVC envelope 혼동 금지
```

| 체크 |
| ---- |
| NhBaseException 사용 |
| Filter 401 ≠ envelope |
| exceptionCode.yml 코드 등록 |

### T4.26 예외 계층(표 그림) — §13.1

```text
                 Throwable
                     │
        ┌────────────┴────────────┐
    Exception                 Error (JVM)
        │
        ├─ NhBaseException          ← commons 표준 (checked)
        │     TYPE: RUNTIME|COMMON|BIZ|AUTH|SERVICE
        │
        ├─ (기타 checked)
        │
        └─ RuntimeException
              ├─ BizException       ← TCF 전용 (비활성 경로)
              ├─ ServiceCommunicationException
              ├─ ServiceHandlerNotFound
              └─ NPE / IAE / …
```

### T4.27 예외 전파(표 그림) — §13.2

```text
 [A] Filter 단계 오류
     JSON/JWT/Header 실패
          │ 예외를 throw 하지 않음
          ▼
     response.sendError(4xx)  → 체인 중단 (MVC Advice 미도달)

 [B] Controller 진입 전 MVC 오류
     (바인딩 등 — 샘플은 커스텀 Resolver로 dto 추출)
          │
          ▼
     프레임워크 기본 처리 또는 Advice (타입에 따라)

 [C] Service / DAO 업무 예외
          │ throw NhBaseException
          ▼
     @Transactional 프록시 ──► rollback (선언 시)
          │
          ▼
     Controller 시그니처 throws 로 전파
          │
          ▼
     @ExceptionHandler(NhBaseException)  ──► NH_NIS_ERR_DTO
          │
          ▼
     Interceptor.afterCompletion(ex) ──► ImageLog EX
          │
          ▼
     Filter finally ──► Context / MDC 정리

 [D] 업무 "정상 분기" (0건·무삭제)
          │ throw 하지 않음
          ▼
     DTOout 결과코드 (RSLT_CD 등) ──► 성공 전문 envelope
```

### T4.28 포착 지점 — §13.3

```text
 [1] DefaultFilter          sendError
 [2] TX Proxy               rollback
 [3] ResponseBodyAdvice     NH_NIS_ERR_DTO
 [4] Interceptor afterComp  ImageLog EX
 [5] Filter finally         Context/MDC clear
```

| 순 | 지점 | 동작 |
| -- | ---- | ---- |
| 1 | DefaultFilter | sendError |
| 2 | TX Proxy | rollback |
| 3 | ResponseBodyArgumentResolver | NH_NIS_ERR_DTO |
| 4 | Interceptor.afterCompletion | ImageLog EX |
| 5 | Filter finally | Context/MDC clear |

### T4.29 업무 예외 시퀀스(표 그림) — §13.4

```text
 Client
   │ POST /서비스ID
   ▼
 Filter OK · Interceptor pre · Aspect before
   ▼
 Controller → Service
   │  throw new NhBaseException("FW0001", args, TYPE.COMMON)
   ▼
 ┌─ @Transactional ─┐
 │  rollback         │
 └────────┬──────────┘
          ▼
 ExceptionHandler → NH_NIS_ERR_DTO (HTTP 500 + body)
          ▼
 Advice → { hdr_nhnis, dto: 에러 }
          ▼
 Aspect after? / Interceptor afterCompletion(EX) / Filter finally
          ▼
 Client
```

### T4.30 예외 vs 결과코드 — §13.5

```text
 시스템·검증 오류 → NhBaseException → NH_NIS_ERR_DTO
 업무 정상 분기(0건 등) → DTOout 결과코드 (예외 아님)
```

| 상황 | 권장 |
| ---- | ---- |
| 시스템·검증 | NhBaseException |
| 업무 정상 분기 | DTOout 결과코드 |

### T4.31 NhExceptionProvider — §13.6

```text
 NhExceptionProvider
  ├─ code → exceptionCode / MessageCache
  └─ NhBaseException 생성 지원
```

| 역할 | 코드→메시지 조회·예외 생성 지원 |

### T4.32 Controller throws — §13.7

```text
 Service throws NhBaseException
      → Controller throws …
           → DispatcherServlet
                → @ExceptionHandler
```

| 항목 | 내용 |
| ---- | ---- |
| 시그니처 | throws Throwable 샘플 존재 |
| checked | 래핑·전파 규칙 준수 |

### T4.33 TCF 예외·체크 — §13.8·13.9

```text
 ☐ BizException(TCF) 미사용
 ☐ Filter단 sendError ≠ MVC단 hdr+dto 에러 구분
```

| 체크 |
| ---- |
| TCF BizException 미사용 |
| Filter 단계와 MVC 단계 응답 형태 구분 |

### T4.34 네이밍(표 그림) — §14

```text
                    서비스 ID = mkcoa8888S0
                              │
     ┌────────────┬───────────┼───────────┬────────────┐
     ▼            ▼           ▼           ▼            ▼
  URL path    메서드명      DTO 접두    SQL ID      sys_comm
 POST /…S0   …S0(...)   …S0DTOin/out  …S0_S0     rms_svc_c
     │            │           │           │
     │     Controller/Service │           DAO 메서드명
     │     (식별번호 단위 클래스: mkcoa8888*)
     │                        │
     └────────────────────────┴──► rdw.mk.co.a/mkcoa8888-ORA.xml
```

| 축 | 규칙 |
| -- | ---- |
| 패키지 | nhnis.mk.{업무}.{세부}.{layer} |
| Mapper | rdw.mk.{업무}.{세부}/{식별}-ORA.xml |
| SQL ID | = DAO 메서드명 |
| 클래스 | 구분자 미포함 |

| 구분자 | 의미 |
| ------ | ---- |
| S/C/U/D/A/R | 조회/등록/수정/삭제/혼합/리포트 |

| 모듈 경계 | 규칙 |
| --------- | ---- |
| ui Catalog.id | = 서비스 ID |
| service | C/S/DAO/DTO 동일 식별 |
| fw | 서비스ID 비즈니스 소유 안 함 |

```text
 ui Catalog.id → service POST /id → nhnis.mk.co.a.* → rdw.mk.co.a/*-ORA.xml
```

### T4.35 네이밍 하지 말 것·체크 — §14.5·14.6

```text
 ❌ /api/업무/list · 클래스명에 S0 · SQL ID≠메서드
 ✅ URL = 메서드 = DTO접두 = rms_svc_c
 ✅ scid=식별 · XML=rdw.mk.{업무}.{세부}/
```

| 금지 |
| ---- |
| /api/업무/list 형태 업무 URL |
| 클래스명에 S0 구분자 |
| SQL ID ≠ 메서드 |

| 체크 |
| ---- |
| URL=메서드=DTO접두=rms_svc_c |
| scid=식별 단위 |
| XML 경로 rdw.mk… |

---

## T5. 데이터 (원본 §15~§17)

```text
 Service ──► §15 DAO ──► MyBatis XML
                │              │
                │              ▼
                │         §16 TB_* (RDW)
                │
                └──────► §17 페이징
                         (pageNo / ROWNUM / count)
```

### T5.0 Part 맵(표 그림)

```text
 Service ──► §15 DAO ──► MyBatis XML
                │              │
                │              ▼
                │         §16 TB_* (RDW)
                │
                └──────► §17 페이징
                         (pageNo / ROWNUM / count)
```

### T5.1 DAO 위치·구성(표 그림) — §15.1

```text
 Service (Application)
      │  Map 파라미터 / List<Map> · int 결과
      ▼
 ┌────────────────────────────────────────────┐
 │  nhnis.mk.co.a.dao.mkcoa{식별}DAO          │
 │  @RDWMapper  (= @Mapper)                   │
 │  메서드명 = SQL ID                          │
 └────────────────────┬───────────────────────┘
                      │ MyBatis
                      ▼
 ┌────────────────────────────────────────────┐
 │  resources/rdw.mk.co.a/mkcoa{식별}-ORA.xml │
 │  namespace = DAO FQCN                       │
 │  <select|insert|update|delete id="SQL ID"> │
 └────────────────────┬───────────────────────┘
                      │
                      ▼
         rdwSqlSessionTemplate → rdwDataSource (H2/Oracle)
```

| 항목 | 값 |
| ---- | -- |
| 패키지 | nhnis.mk.{업무}.{세부}.dao |
| 인터페이스 | mkcoa{식별}DAO (구분자 제외) |
| XML | mkcoa{식별}-ORA.xml |
| MapperScan | nhnis.mk.**.dao |

### T5.2 메서드·SQL ID — §15.2

```text
[서비스ID]_[DML][순번]
[서비스ID]_[DML][순번]_count
```

| 예 | DML | 반환 |
| -- | --- | ---- |
| mkcoa8888S0_S0 | SELECT | List<Map> |
| mkcoa8888S0_S0_count | COUNT | int |
| mkcoa8888D0_D0 | DELETE | int |

| 규칙 | 설명 |
| ---- | ---- |
| 메서드=id | 문자 단위 일치 |
| 입력 | Map |
| 출력 | List/int · **DTO 금지** |

### T5.3 샘플 DAO — §15.3

```text
 mkcoa8888DAO  S0_S0(+count)·D0_D0 → TB_FW_IMAGE_LOG
 mkcoa5530DAO  S0_S0(+count)       → TB_MK_CO_A_5530
 mkcoa9999DAO  S0_S0(+count)       → TB_CR_AH_SALES_TIP_RACT
```

| DAO | SQL ID | 용도 |
| --- | ------ | ---- |
| mkcoa8888DAO | S0_S0(+count), D0_D0 | ImageLog |
| mkcoa5530DAO | S0_S0(+count) | 안내항목 |
| mkcoa9999DAO | S0_S0(+count) | 영업팁 |

### T5.4 Mapper XML 계약 — §15.4

```text
 <mapper namespace="nhnis.mk.co.a.dao.mkcoa{식별}DAO">
   <select id="{서비스ID}_S0" parameterType/resultType=HashMap>
     SELECT /* SQL ID */ …  (ROWNUM 페이징)
   </select>
 </mapper>
```

| 항목 | 규칙 |
| ---- | ---- |
| namespace | DAO FQCN |
| parameter/result | HashMap 명시 |
| 주석 | /* SQL ID */ |
| 페이징 | Oracle ROWNUM |

### T5.5 인프라 Bean(표 그림) — §15.5

```text
RdwDataSourceConfig
├─ rdwDataSource              (@Primary, Hikari)
├─ rdwSqlSessionFactory       (+ MybatisLogInterceptor)
├─ rdwSqlSessionTemplate
└─ rdwTransactionManager      ← Service @Transactional 이 사용
```

### T5.6 Service↔DAO 책임(표 그림) — §15.6

```text
 Service
  ├─ DTOin → Map (조건·offset·pageSize·guidList)
  ├─ DAO 호출 (목록 + count 등)
  ├─ Map 행 → DTOSub / DTOout
  └─ 페이징·업무 검증·결과코드

 DAO
  ├─ SQL 실행만
  └─ 비즈니스 if / DTO / HTTP 금지
```

### T5.7 DAO 금지·체크 — §15.7·15.8

```text
 ❌ 메서드≠XML id · DAO에 DTO · C→DAO · 임의 mapper/ 경로
 ✅ @RDWMapper · namespace=FQCN · Map · TX=Service
```

| 금지 |
| ---- |
| 메서드 ≠ XML id |
| DAO에 DTOin/out |
| Controller→DAO |
| XML을 mapper/ 임의 경로 |
| namespace ≠ FQCN |

### T5.8 테이블 DS·위치(표 그림) — §16.1

```text
 pdmk-ui          (테이블 없음)
      │ HTTP
      ▼
 pdmk-service
      ├─ MyBatis DAO ──► rdwDataSource ──► TB_* (업무·조회)
      └─ (내장) pdmk-fw
           ├─ ImageLogHandler (JdbcTemplate) ──► TB_FW_IMAGE_LOG
           └─ MessageLoader (옵션) ──► framework.message.tableName[]
```

| 항목 | 내용 |
| ---- | ---- |
| local | H2 + sql.init |
| 폐쇄망 | Oracle · sql.init off |
| 운영 DDL | 앱 미생성 |

### T5.9 테이블 소유(표 그림) — §16.2

```text
                    RDW (H2 / Oracle)
  ┌─────────────────────┬──────────────────────┐
  │ TB_FW_IMAGE_LOG     │ TB_CR_AH_SALES_TIP…  │
  │  (감사 GUID)        │ TB_MK_CO_A_5530      │
  └──────────┬──────────┴──────────┬───────────┘
             │                     │
    fw ImageLogHandler      service MyBatis DAO
    (JdbcTemplate I/U)      (mkcoa8888/9999/5530)
             │                     │
             └──────────┬──────────┘
                        ▼
                 pdmk-service
                        ▲
                        │ HTTP only
                   pdmk-ui (DB 없음)
```

| 테이블 | 주 접근 | CRUD |
| ------ | ------- | ---- |
| TB_FW_IMAGE_LOG | fw+mkcoa8888 | I/U(fw), S/D(svc) |
| TB_CR_AH_SALES_TIP_RACT | mkcoa9999 | S |
| TB_MK_CO_A_5530 | mkcoa5530 | S |
| 메시지 번들(옵션) | MessageLoader | S 기동 |

### T5.10 명명·소유 — §16.3

```text
 TB_FW_* · TB_MK_{업무}_{세부}_* · TB_{타시스템}_*
 스키마 소유 = RDW · 앱 ≠ 테이블 오너 · Alias T1…
```

| 패턴 | 예 |
| ---- | -- |
| TB_FW_* | TB_FW_IMAGE_LOG |
| TB_MK_{업무}_{세부}_* | TB_MK_CO_A_5530 |
| TB_{타시스템}_* | TB_CR_AH_SALES_TIP_RACT |

### T5.11 IMAGE_LOG 컬럼 — §16.4

```text
 TB_FW_IMAGE_LOG
  GUID←std_gbl_id · SERVICE_ID←rms_svc_c · SCREEN_ID←scid
  OPTR_ENO · CLIENT_IP · REQUEST/RESPONSE_TIME · EXCEPTION_*
  PRE INSERT / POST UPDATE / EX UPDATE (fw ImageLogHandler)
```

| 컬럼 | 매핑 |
| ---- | ---- |
| GUID | std_gbl_id |
| SERVICE_ID | rms_svc_c |
| SCREEN_ID | scid |
| OPTR_ENO | optr_eno |
| CLIENT_IP | tr_trm_ipadr |
| REQUEST/RESPONSE_TIME | tr_dtm / 후처리 |
| EXCEPTION_* | 예외 정보 |

| 단계 | SQL |
| ---- | --- |
| PRE | INSERT |
| POST | UPDATE RESPONSE_TIME |
| EX | UPDATE 예외 (없으면 INSERT) |
| S/D | mkcoa8888 |

### T5.12 SALES_TIP 컬럼 — §16.5

```text
 TB_CR_AH_SALES_TIP_RACT
  PK(TRT_BRC, TRTMN_ENO, SALZ_TIP_KDC, BAS_DT)
  → mkcoa9999S0 (목록, 페이징 갭)
```

| 컬럼 | 의미 |
| ---- | ---- |
| TRT_BRC / TRTMN_ENO / SALZ_TIP_KDC / BAS_DT | PK |
| PRTO_CN / INQ_CN / INP_CN | 내용 |

### T5.13 TB_MK_CO_A_5530 — §16.6

```text
 TB_MK_CO_A_5530  PK(L5101, L5103)
  L5102 항목명 · L5104←trtBrc · L5103←basDt
  → mkcoa5530S0 페이징 ○
```

| 컬럼 | 의미 |
| ---- | ---- |
| L5101 / L5103 | PK (항목·기준일) |
| L5102 | 항목명 |
| L5104 | 취급점 (trtBrc) |

### T5.14 메시지 번들·매트릭스 — §16.7·16.8

```text
 [옵션] CRM_MSG_C/MSG_CNTN → MessageCache (H2 schema 미포함)
 전거래 ImageLog I/U | 8888 S/D | 5530 S | 9999 S
```

| 논리 컬럼 | 용도 |
| --------- | ---- |
| CRM_MSG_C | 코드 |
| MSG_CNTN | 템플릿 |

| 서비스 | 테이블 | DML |
| ------ | ------ | --- |
| (전 거래) ImageLog | TB_FW_IMAGE_LOG | I/U |
| mkcoa8888S0/D0 | 동상 | S/D |
| mkcoa5530S0 | TB_MK_CO_A_5530 | S |
| mkcoa9999S0 | TB_CR_AH_SALES_TIP_RACT | S |

### T5.15 환경·경계 — §16.9·16.10

```text
 local: schema.sql+data.sql | 폐쇄망: DBA·sql.init off
 ❌ ui→DB · 앱 운영 DDL · ImageLog실패→업무강제실패
```

| 환경 | 스키마 |
| ---- | ------ |
| local | schema.sql + data.sql |
| 폐쇄망 | DBA/기존 · init off |

| 금지 |
| ---- |
| ui→DB 직결 |
| 운영 DDL을 앱이 생성 |
| ImageLog 실패로 업무 강제 실패 |

### T5.16 페이징(표 그림) — §17

```text
 UI (pageNo, pageSize)
      │  hdr_nhnis + dto
      ▼
 Service
      ├─ normalize: pageNo≥1, pageSize 1~100 (기본 20)
      ├─ offset = (pageNo - 1) * pageSize
      ├─ DAO *_count(param) → totalCount
      ├─ DAO *_S0(param)    → rows (ROWNUM 슬라이스)
      └─ DTOout: Sub[] + size + pageNo + pageSize + totalCount + totalPages
      ▼
 Mapper XML (Oracle ROWNUM / H2 MODE=Oracle)
```

| 서비스 | 페이징 |
| ------ | ------ |
| mkcoa5530/8888 목록 | ○ |
| mkcoa9999S0 | ❌ 전량 갭 |

| 규칙 | 내용 |
| ---- | ---- |
| 계산 | Service |
| SQL | ROWNUM 래핑 |
| 계약 | pageNo,pageSize,totalCount,totalPages,size |

---

## T6. 횡단 (원본 §18~§22)

```text
          ┌─ §18 보안 (JWT / Security / CORS)
 요청 ──►├─ §19 AOP (Aspect / Interceptor / Plugin)
          ├─ §20 타임아웃 (HTTP / TX / JWT)
          ├─ §21 TX로그 (PdmkTxLog / ImageLog / SQL)
          └─ §22 캐시 (MessageCache / Body 버퍼)
```

### T6.1 보안(표 그림) — §18

```text
 Browser
   │  (직접 service 호출 시 CORS·JWT 정책 적용)
   ▼
 pdmk-ui :8090          ← 인증 없음. HTTP 릴레이만 (Bearer 미전달 현황)
   │
   ▼
 pdmk-service :8080
   ├─ DefaultFilter (fw)     ← 비-local: Bearer Access JWT 필수 → 401
   ├─ SecurityFilterChain    ← STATELESS, CSRF off, **permitAll**
   ├─ Interceptor / Aspect   ← 인가 아님 (선후·로그)
   └─ Controller → Service → DAO  ← MyBatis #{} 바인딩
         │
         ▼
      RDW (계정·네트워크 권한은 인프라)
```

| 층 | 역할 |
| -- | ---- |
| Filter JWT | 실인증(비-local) |
| Security | 샘플 전 URL 허용 |
| UI | 릴레이 |

### T6.2 Security 구성·갭 — §18.2·18.8

```text
 SecurityConfig: permitAll · STATELESS · CSRF off
 fw commons.security.enabled=false
 갭: UI Authorization 미전달 · local 시크릿 운영 금지
```

| 항목 | 값 |
| ---- | -- |
| authorize | permitAll |
| session | STATELESS |
| CSRF | off |
| fw Security | 기본 OFF · 이중 금지 |
| 갭 | Bearer 미전달 · local 시크릿 운영 금지 |

### T6.3 CORS·헤더 — §18

```text
 Browser ─(CORS spring.mvc.cors)─► ui ─(?Authorization)─► service
 비-local: DefaultFilter JWT 검증 · UI Bearer 미전달=갭 (copy §18)
```

| 항목 | 샘플 전제 |
| ---- | --------- |
| CORS | 필요 시 Security/앱 설정 |
| Authorization | 비-local Filter 검증 · UI 미전달 갭 |

### T6.4 AOP(표 그림) — §19

```text
 Servlet Filter          DefaultFilter          ← AOP 아님 (Servlet)
      │
 Security FilterChain                            ← AOP 아님
      │
 DispatcherServlet
      │
 HandlerInterceptor      ServicePreventionInterceptor  ← Web MVC (시스템 선후)
      │
 Spring AOP @Before/@After
                         BizPrePostAspect              ← **활성** 업무 선후
      │
 Controller
      │
 Spring AOP (TX)         @Transactional advisor        ← Service 경계(권장)
      │
 MyBatis Plugin          MybatisLogInterceptor          ← SQL 계측 (AOP 아님)
      │
 (비활성) Spring AOP @Around
                         TCFAspect + @TcfTransaction   ← tcf.enabled=true 만
```

| 메커니즘 | 클래스 | 활성 |
| -------- | ------ | ---- |
| AOP | BizPrePostAspect | legacy-web |
| Interceptor | ServicePreventionInterceptor | legacy-web |
| Plugin | MybatisLogInterceptor | 항상 |
| TX | @Transactional | Service |
| TCFAspect | — | OFF |

### T6.5 AOP 경계 — §19

```text
 BizAspect=업무로그 ≠ DB/ImageLog/TX
 Interceptor=GUID·ImageLog ≠ 업무규칙
 @Transactional=commit ≠ HTTP
 TCFAspect = OFF
```

| 담당 | 비담당 |
| ---- | ------ |
| BizAspect: C 로그 | DB·ImageLog·TX |
| Interceptor: GUID·ImageLog | 업무 규칙 |
| TX Proxy: commit | HTTP |

### T6.6 타임아웃(표 그림) — §20

```text
 Browser
   │  (브라우저/네트워크 — 본 스택 밖)
   ▼
 pdmk-ui
   │  pdmk.ui.timeout-ms = 10000   ← 설정·화면 노출
   │  ※ 현재 RestClient에 미적용(갭) — 권장: HTTP 클라이언트에 연결
   ▼
 pdmk-service  (+ pdmk-fw)
   │  ┌─ JWT access-token-expiration (ms)     [인증 수명]
   │  ├─ @Transactional(timeout = 초)         [DB TX · 권장]
   │  ├─ slow-transaction-ms (WARN, TCF문서)  [성능 경고 ≠ rollback]
   │  └─ sys_comm.ttl_ug_ync                  [전문 플래그 ≠ Spring TX]
   ▼
 pdmk-fw 연동(선택)
   │  apigw.connect/response-timeout (초)
   │  storage.fos.connect/response-timeout
   ▼
 RDW / 외부 시스템
```

| 계층 | 수단 | 비고 |
| ---- | ---- | ---- |
| UI | timeout-ms | RestClient **미연결** |
| DB | @Transactional(timeout) | 초 |
| JWT | access-token-expiration | ms |
| ttl_ug_ync | 전문 필드 | TX 미연동 |

### T6.7 로그(표 그림) — §21

```text
 Browser ──(선택)──► pdmk-ui  RelayResult(elapsedMs)     [ui 중계 소요]
                         │
                         ▼
              pdmk-service + pdmk-fw
 ┌──────────────────────────────────────────────────────────┐
 │ ① MDC / ThreadContext                                    │
 │    guid · ip · userId · serviceId  (DefaultFilter)       │
 │    sqlId                       (MybatisLogInterceptor)  │
 ├──────────────────────────────────────────────────────────┤
 │ ② 애플리케이션 거래 로그 (Slf4j + PdmkTxLog 메시지)         │
 │    Interceptor → Aspect → Controller → Service           │
 ├──────────────────────────────────────────────────────────┤
 │ ③ 이미지로그 DB  TB_FW_IMAGE_LOG  (ImageLogHandler)       │
 │    PRE INSERT → POST UPDATE → EX UPDATE                  │
 ├──────────────────────────────────────────────────────────┤
 │ ④ SQL 로그  MybatisLogInterceptor (debug + MDC sqlId)     │
 └──────────────────────────────────────────────────────────┘
```

| 채널 | 주체 | 내용 |
| ---- | ---- | ---- |
| 콘솔/파일 | PdmkTxLog | system/biz |
| DB 감사 | ImageLogHandler | GUID 행 |
| SQL | MybatisLogInterceptor | sqlId |

### T6.8 캐시(표 그림) — §22

```text
 요청 Body 버퍼 (요청 스코프)          메시지/사전 (프로세스 스코프)
 ┌─────────────────────────┐          ┌──────────────────────────────┐
 │ CachedBodyHttpServlet   │          │ MessageCache (ConcurrentMap) │
 │ Request  — fw Filter    │          │  ← MessageLoader @PostConstruct│
 └─────────────────────────┘          │ exceptionCode.yml (Environment)│
                                      │ TransactionCatalog (ui only) │
                                      └──────────────────────────────┘
```

| 종류 | 수명 |
| ---- | ---- |
| MessageCache | JVM·기동 적재 |
| CachedBody | 요청 |
| exceptionCode.yml | 기동 |
| Catalog | UI 프로세스 |

---

## T7. 플랫폼 (원본 §23~§25)

```text
  §23 Spring yml/프로파일
         │
         ▼
  §24 Gradle Wrapper · 의존 · 산출물
         │
         ▼
  §25 기동 · WAS · local/폐쇄망 전환
```

### T7.1 Boot·스캔 — §23.1

```text
 PdmkApplication
   scanBasePackages = "nhnis"
        │
        ├─ nhnis.mk.*          ← service 업무·config·Aspect
        └─ nhnis.fw.*          ← commons Filter/Interceptor/JWT …
             └─ nhnis.fw.tcf.* ← @ConditionalOnProperty → 대부분 미생성
```

| 모듈 | Main | 스캔 |
| ---- | ---- | ---- |
| service | PdmkApplication | nhnis |
| fw | 없음 | 편입 |
| ui | PdmkUiApplication | fw 미의존 |

### T7.2 설정 적층(표 그림) — §23.2

```text
 환경변수 / 시스템 프로퍼티     (최상위·시크릿)
      │
 spring.profiles.active=local|dev|…
      │
 application.yml
   ├─ 공통 블록 (profiles·DS·nhnis.fw·sql.init …)
   ├─ --- on-profile: local   → jwt.*
   └─ --- on-profile: dev     → jwt.*
      │
 spring.config.import
   └─ classpath:exceptionCode.yml   → nhnis.exception.*
      │
 (참고) pdmk-fw application-fw-defaults.yml
   └─ 라이브러리 샘플 — 앱 yml이 덮어씀. 자동 import 아님
```

### T7.3 프로파일 — §23

```text
 [local] H2 · JWT 스킵 · hdr 합성 · Central
 [비-local/폐쇄망] Oracle · JWT ON · hdr 필수 · Nexus · WAS
```

| 프로파일 | 특징 |
| -------- | ---- |
| local | H2 · JWT 스킵 · 헤더 합성 |
| (비-local) | Oracle · JWT ON |

### T7.4 설정 키 — §23.9

```text
 spring.*
   profiles / config.import / datasource.rdw / sql.init / mvc.cors / application.name

 nhnis.fw.*
   tcf.enabled
   commons.filter.enabled
   commons.legacy-web.enabled
   commons.security.enabled   (기본 미사용)

 jwt.*
   enabled / secret / access-token-expiration

 framework.message.*
   enabled / dataSource / tableName

 pdmk.ui.*          (ui 전용)
   target-base-url / timeout-ms

 nhnis.exception.*  (exceptionCode.yml)
```

| 키 | local | 의미 |
| -- | ----- | ---- |
| spring.profiles.active | local | 프로파일 |
| nhnis.fw.tcf.enabled | false | TCF OFF |
| commons.filter.enabled | true | Filter |
| commons.legacy-web.enabled | true | Interceptor·Aspect |
| commons.security.enabled | false | fw Security OFF |
| jwt.secret / PDMK_JWT_SECRET | 로컬기본 | 시크릿 |
| spring.datasource.rdw | H2 | DS |
| framework.message.enabled | false | MessageCache |
| pdmk.ui.target-base-url | :8080 | 릴레이 |
| pdmk.ui.timeout-ms | 10000 | **갭** |

### T7.5 Gradle(표 그림) — §24

```text
 nsight-tcf-framework/
 ├─ pdmk-fw/          ← 단독 Gradle 루트 (java-library JAR)
 │    settings.gradle   rootProject.name = pdmk-fw
 │    build.gradle
 │    gradlew.bat + gradle/wrapper (8.10.1)
 │
 ├─ pdmk-service/     ← 단독 Gradle 루트 + fw composite
 │    settings.gradle   include ':pdmk-fw' → ../pdmk-fw
 │    build.gradle      implementation project(':pdmk-fw')
 │    gradle.properties UTF-8 JVM args
 │    script/build.bat · RUN.bat
 │
 └─ pdmk-ui/          ← 단독 Gradle 루트 (Boot JAR, fw 미포함)
      settings.gradle   rootProject.name = pdmk-ui
      build.gradle
```

| 모듈 | 산출 |
| ---- | ---- |
| fw | jar (bootJar off) |
| service | war (bootWar off) |
| ui | pdmk-ui.jar |

### T7.6 Boot 스타터 핀 — §24.2

```text
 Plugin Boot 3.5.14 · JDK 21 · Wrapper 8.10.1
 fw 스타터 버전 핀 상이 가능 → 의존 정렬 주의
 local=Maven Central / 폐쇄망=Nexus
```

| 항목 | 내용 |
| ---- | ---- |
| Plugin Boot | 3.5.14 |
| fw 의존 스타터 | 버전 핀 상이 가능(3.5.10 등) — 문서화 갭 주의 |
| 저장소 | local Central / 폐쇄망 Nexus |

### T7.7 환경·기동(표 그림) — §25

```text
 [1] pdmk-fw
      gradlew jar
         │
 [2] pdmk-service  (fw를 project 의존으로 함께 빌드)
      script\build.bat   → clean build → build/libs/*.war
      RUN.bat / script\run.bat → bootRun :8080
         │
 [3] pdmk-ui
      script\build.bat   → build/libs/pdmk-ui.jar
      RUN.bat / bootRun  → :8090
      (옵션) script\run.bat jar → java -jar pdmk-ui.jar
```

| 환경 | UI | Service | DB |
| ---- | -- | ------- | -- |
| local | 8090 | 8080 | H2 |
| 폐쇄망 | 배포 | WAS WAR | Oracle |

### T7.8 컨테이너 — §25

```text
 Docker / K8s 정의 없음 (갭) · 현재 bootRun · WAS WAR
```

| 항목 | 현황 |
| ---- | ---- |
| Docker/K8s | **없음**(갭) |

---

## T8. 지침·실무 (원본 §26~§29)

```text
  §26 경계 ──► 갭 · Do/Don't · 새 거래 체크
  §27 참고 ──► README · 상세 MD
  §28 용어 ──► 키워드 → 절
  §29 실무 ──► 역할 · 환경 · 장애 · 영향
```

### T8.1 갭(표 그림) — §26.0

```text
  [데이터] mkcoa9999 전량 ───────► §17 페이징
  [UI]     timeout-ms 미연결 ────► §20 타임아웃
  [보안]   Bearer 미전달 ────────► §18 보안
  [보안]   permitAll 샘플 ───────► §18.8
  [TX]     @Transactional 일부미명시 ► §11.4
  [FW]     TCF 소스 잔존 ────────► §9.7
  [배포]   컨테이너 없음 ────────► §25.8
```

| # | 갭 |
| - | -- |
| 1 | mkcoa9999 페이징 없음 |
| 2 | UI RestClient timeout 미연결 |
| 3 | Authorization 미전달 |
| 4 | Security permitAll |
| 5 | 일부 @Transactional 미명시 |
| 6 | TCF 소스 잔존 |
| 7 | Docker/K8s 없음 |

### T8.2 Do / Don't — §26.1~26.2

```text
 ✅ 세모듈 동시 · CONVENTION · TX=Service · 페이징 · commons ON · WAR
 ❌ 한모듈만 · /api 업무URL · C/DAO TX · 전량SELECT 표준 · TCF 혼용
```

| 해야 할 것 | 하지 말 것 |
| ---------- | ---------- |
| 세 모듈 동시 검토 | 한 모듈만 변경 |
| CONVENTION 네이밍 | 업무 REST /api URL |
| TX=Service | C/DAO에 TX |
| 페이징 계약 | 전량 SELECT 표준 |
| commons ON·TCF OFF | 이중 경로 |
| WAR 전제 | 합의 없이 bootJar 운영 |
| hdr+dto | 루트 업무 필드 |
| Map DAO | DAO에 DTO |

### T8.3 TCF 스위치(표 그림) — §26.3

```text
  nhnis.fw.tcf.enabled
         │
    false │ (PDMK 기본)          true │ (비권장·혼용금지)
         ▼                           ▼
   commons 경로만                 TCFAspect + STF/ETF
   Filter+Interceptor+BizAspect   + commons 이중 경로 위험
```

### T8.4 새 거래(표 그림) — §26.4

```text
 ① 네이밍·패키지 (§14)
        │
        ▼
 ② 전문 DTO (§10) ──────────────────┐
        │                           │ ui 샘플 JSON
        ▼                           │
 ③ C/S/DAO/XML (§7·§15)             │
        │                           │
        ▼                           │
 ④ 페이징·TX·테이블 (§17·§11·§16)   │
        │                           │
        ▼                           ▼
 ⑤ UI 카탈로그·화면 ◄───────────────┘
        │
        ▼
 ⑥ (필요 시) fw commons만
```

| 체크 |
| ---- |
| 서비스ID 11자리 규칙 |
| DTOin/out/Sub |
| SQL ID=메서드 |
| @Transactional |
| 목록이면 페이징 |
| UI id 등록 |

### T8.5 참고 문서 — §27

```text
 copy.md(서술정본) · 표 아키텍처(본문서)
 네이밍원칙 · MK-NAMING_CONVENTION · pdmk-*/README
```

| 문서 | 용도 |
| ---- | ---- |
| 통합 아키텍처 정의서 copy.md | 서술 정본 |
| 네이밍원칙 · MK-NAMING_CONVENTION | 이름 |
| pdmk-*/README | 모듈 기동 |
| (개별) 에러·TX 보충 md | 상세 |

### T8.6 용어·색인 — §28

```text
  [스택] PDMK = fw + service + ui     → §1·§2·§8
  [경로] commons ON / TCF OFF         → §9
  [계약] 서비스ID · hdr+dto           → §10·§14
  [데이터] DAO · TB_* · 페이징        → §15~§17
  [횡단] JWT · AOP · 로그 · 캐시      → §18~§22
  [실무] 갭 · 체크리스트 · 장애추적   → §26·§29
```

| 용어 | 한줄 |
| ---- | ---- |
| PDMK | fw+service+ui |
| commons / TCF | ON / OFF |
| 서비스 ID | URL=메서드 |
| RDW | 데이터소스·스키마 |
| ImageLog | GUID 감사 테이블 |
| PdmkTxLog | 로그 포맷 유틸 |
| hdr_nhnis / sys_comm | 공통 헤더 |
| NH_NIS_ERR_DTO | 에러 본문 |
| legacy-web | Interceptor·Resolver·Aspect 스위치 |

### T8.7 샘플 거래 맵 — §28.4

```text
 mkcoa5530S0 안내항목 페이징○ · mkcoa8888S0/D0 ImageLog
 mkcoa9999S0 영업팁 페이징❌ (갭)
```

| 서비스 | 프로그램 | 페이징 |
| ------ | -------- | ------ |
| mkcoa5530S0 | 안내항목 | ○ |
| mkcoa8888S0/D0 | ImageLog 조회/삭제 | ○/— |
| mkcoa9999S0 | 영업팁 | ❌ |

### T8.8 설정 키→절 — §28.5

```text
 tcf → T2/T3/T8 · filter/legacy-web → T2
 rdw DS → T5/T7 · jwt → T2/T6 · pdmk.ui → T3/T6/T7
```

| 키 | 본문 |
| -- | ---- |
| tcf.enabled | T2·T3·T8 |
| filter / legacy-web | T2 |
| datasource.rdw | T5·T7 |
| jwt.* | T2·T6 |
| pdmk.ui.* | T3·T6·T7 |
| framework.message | T4·T6 |

### T8.9 역할별 읽기 — §29.1

```text
 신규 ──► §1→§3→§4→§8→§14
 업무 ──► §26.4→§10/14/15/17→§11
 FW   ──► §9→§5/6→§18/19
 운영 ──► §23/24/25→§29.2
 장애 ──► §29.3→§21/12/13
 리뷰 ──► §26.0/26.1/26.2→§29.4
```

| 역할 | 경로 |
| ---- | ---- |
| 신규 | T1→T2→T3→T4 |
| 업무 | T8.4→T4→T5 |
| FW | T3→T2→T6 |
| 운영 | T7·T8.10 |
| 장애 | T8.11→T6 |
| 리뷰 | T8.1~2 · T8.12 |

### T8.10 환경 매트릭스 — §29.2

```text
  [local]                         [폐쇄망/WAS]
  ┌──────────────┐                ┌──────────────┐
  │ ui :8090     │                │ ui :8090     │
  │ service:8080 │                │ service WAR  │
  │ H2 mem       │                │ Oracle RDW   │
  │ JWT 스킵     │                │ JWT 검증 ON  │
  │ Central      │                │ Nexus        │
  └──────────────┘                └──────────────┘
         │ 설정 반입 금지 │
         └────── ✕ ──────┘
```

| 항목 | local | 폐쇄망 |
| ---- | ----- | ------ |
| DB | H2 mem | Oracle |
| sql.init | always | off |
| JWT | 스킵 | ON |
| hdr | 합성 가능 | 필수 |
| 저장소 | Central | Nexus |
| 기동 | bootRun | WAS WAR |
| UI Bearer | — | 전달 필요(갭) |

### T8.11 장애(표 그림) — §29.3

```text
 증상 발생
    │
    ├─ UI만? ──────────► target-base-url / 릴레이 (§3·§8.3)
    ├─ 401? ───────────► Filter JWT / local / Bearer (§6·§18)
    ├─ 전문 파싱? ─────► hdr+dto / Resolver (§10)
    ├─ 에러코드 응답? ─► NhBaseException / yml (§12·§13)
    ├─ SQL? ───────────► DAO=SQL ID / DS rdw (§15)
    ├─ 목록 폭주? ─────► 페이징·9999 갭 (§17·§26.0)
    ├─ TX 의외? ───────► @Transactional / ImageLog (§11)
    └─ Bean 없음? ─────► scan=nhnis / :pdmk-fw (§23·§24)
```

| 증상 | 점검 |
| ---- | ---- |
| UI만 실패 | target-base-url · 포트 |
| 401 | JWT · Bearer · profile |
| 400 | hdr · Body · JSON |
| 에러 dto | exceptionCode · NhBase |
| SQL 실패 | namespace · SQL ID · DS |
| 목록 폭주 | 9999 페이징 갭 |
| 롤백 안 됨 | @Transactional |
| Bean 없음 | 스캔 · conditional |

### T8.12 변경 영향 — §29.4

```text
  변경점
    │
    ├─ 서비스ID/DTO/DAO ──► service●  ui●(카탈로그)
    ├─ 헤더 필드 ─────────► fw● service● ui●
    ├─ Filter/JWT ────────► fw●  service○  ui○(Bearer)
    ├─ 테이블 ────────────► service●  fw○(ImageLog)
    ├─ 페이징 계약 ───────► service●  ui●
    └─ Gradle/Boot ───────► fw● service● ui●
```

| 변경 | fw | service | ui |
| ---- | -- | ------- | -- |
| 서비스ID·DTO·DAO | — | ● | ● |
| 헤더 | ● | ● | ● |
| Filter/JWT | ● | ○ | ○ |
| 테이블 | ○ | ● | — |
| 페이징 | — | ● | ● |
| Gradle/Boot | ● | ● | ● |
| Security | ○ | ● | ○ |
| ImageLog | ● | ● | — |

### T8.13 포트(표 그림) — §29.5

```text
  :8090                :8080
 ┌────────┐  POST     ┌──────────┐
 │pdmk-ui │ ────────► │pdmk-svc  │
 │ relay  │ /{서비스ID}│ + fw     │
 └───┬────┘           └────┬─────┘
     │                     │
  /api/relay            MyBatis
  /api/config              │
                           ▼
                        RDW DB
```

---

## 부록. 서술 정본 매핑

| 본문 | copy.md |
| ---- | ------- |
| T1 | §1~§3 |
| T2 | §4~§6 |
| T3 | §7~§9 |
| T4 | §10~§14 |
| T5 | §15~§17 |
| T6 | §18~§22 |
| T7 | §23~§25 |
| T8 | §26~§29 |

| 원본 세부 → 본문 예 |
| ------------------- |
| §5.1 시퀀스 | T2.4 |
| §6.1 필터 맵 | T2.12 |
| §10.3 sys_comm | T4.3 |
| §11 TX | T4.8~16 |
| §15~16 DAO·테이블 | T5 |
| §17 페이징 | T5.16 |
| §26.0 갭 | T8.1 |
| §29.2 환경 | T8.10 |

| 문서 | 용도 |
| ---- | ---- |
| [통합 아키텍처 정의서 copy.md](./통합%20아키텍처%20정의서%20copy.md) | 서술·체크리스트 정본 |
| [통합 아키텍처 정의서.md](./통합%20아키텍처%20정의서.md) | 원본 |
| [pdmk-fw/README.md](../../pdmk-fw/README.md) | FW |
| [pdmk-service/README.md](../README.md) | Service |
| [pdmk-ui/README.md](../../pdmk-ui/README.md) | UI |

---

*끝 — 원본 §1~§29 전 주제를 표·TEXT 표 그림으로 재정의.*
