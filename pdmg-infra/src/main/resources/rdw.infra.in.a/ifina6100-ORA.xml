<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nhnis.infra.in.a.persistence.dao.ifina6100DAO">
  <select id="ifina6100S0_S1" resultType="java.util.HashMap" parameterType="java.util.HashMap">
    SELECT PROFILE_ID AS PROFILE_ID, TARGET_TYPE_CD AS TARGET_TYPE_CD, TARGET_ID AS TARGET_ID,
           OPS_HOURS_CD AS OPS_HOURS_CD, HA_YN AS HA_YN, HA_MODE_CD AS HA_MODE_CD, CLUSTER_YN AS CLUSTER_YN,
           DR_YN AS DR_YN, DR_MODE_CD AS DR_MODE_CD, RTO_MINUTES AS RTO_MINUTES, RPO_MINUTES AS RPO_MINUTES,
           BACKUP_YN AS BACKUP_YN, MONITORING_YN AS MONITORING_YN, REMARK AS REMARK,
           REG_USER_ID AS REG_USER_ID, REG_DTM AS REG_DTM, CHG_USER_ID AS CHG_USER_ID, CHG_DTM AS CHG_DTM
      FROM TB_IF_AVAIL_PROFILE_PILOT
     WHERE TARGET_TYPE_CD = #{targetTypeCd} AND TARGET_ID = #{targetId}
  </select>
  <select id="ifina6100S0_exists" resultType="int" parameterType="java.util.HashMap">
    SELECT COUNT(*) FROM TB_IF_AVAIL_PROFILE_PILOT
     WHERE TARGET_TYPE_CD = #{targetTypeCd} AND TARGET_ID = #{targetId}
  </select>
  <insert id="ifina6100U0_insert" parameterType="java.util.HashMap">
    INSERT INTO TB_IF_AVAIL_PROFILE_PILOT (
      PROFILE_ID, TARGET_TYPE_CD, TARGET_ID, OPS_HOURS_CD, HA_YN, HA_MODE_CD, CLUSTER_YN,
      DR_YN, DR_MODE_CD, RTO_MINUTES, RPO_MINUTES, BACKUP_YN, MONITORING_YN, REMARK, REG_USER_ID, REG_DTM
    ) VALUES (
      #{profileId}, #{targetTypeCd}, #{targetId}, #{opsHoursCd}, #{haYn}, #{haModeCd}, #{clusterYn},
      #{drYn}, #{drModeCd}, #{rtoMinutes}, #{rpoMinutes}, #{backupYn}, #{monitoringYn}, #{remark}, #{regUserId}, #{regDtm}
    )
  </insert>
  <update id="ifina6100U0_update" parameterType="java.util.HashMap">
    UPDATE TB_IF_AVAIL_PROFILE_PILOT
       SET OPS_HOURS_CD=#{opsHoursCd}, HA_YN=#{haYn}, HA_MODE_CD=#{haModeCd}, CLUSTER_YN=#{clusterYn},
           DR_YN=#{drYn}, DR_MODE_CD=#{drModeCd}, RTO_MINUTES=#{rtoMinutes}, RPO_MINUTES=#{rpoMinutes},
           BACKUP_YN=#{backupYn}, MONITORING_YN=#{monitoringYn}, REMARK=#{remark},
           CHG_USER_ID=#{chgUserId}, CHG_DTM=#{chgDtm}
     WHERE TARGET_TYPE_CD=#{targetTypeCd} AND TARGET_ID=#{targetId}
  </update>
</mapper>
