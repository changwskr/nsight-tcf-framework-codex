# local bootRun — 환경변수 예시 (비밀값 Git 금지)
# 사용: set 후 gradle bootRun 또는 IDE Run Configuration

# nsight.txlog.path (Gradle bootRun이 자동 주입 — 수동 오버라이드 시)
# NSIGHT_TXLOG_PATH=C:/Programming(23-08-15)/nsight-tcf-framework/data/nsight-txlog

# Spring (루트 build.gradle 기본값)
# SPRING_PROFILES_ACTIVE=local
