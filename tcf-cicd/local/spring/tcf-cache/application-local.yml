# local — bootRun / EhCache JCache
spring:
  cache:
    type: jcache
    jcache:
      config: classpath:ehcache.xml

nsight:
  tcf:
    cache:
      enabled: true
      config-location: classpath:ehcache.xml
