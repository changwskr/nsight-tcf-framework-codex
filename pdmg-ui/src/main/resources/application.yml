server:
  port: 8090

spring:
  application:
    name: pdmg-ui

pdmg:
  ui:
    # 전문 테스트 대상. 화면에서 바꿔가며 시험할 수 있고 여기 값은 초기값이다.
    target-base-url: http://localhost:8080
    timeout-ms: 10000

logging:
  level:
    nhnis.mg.ui: debug
