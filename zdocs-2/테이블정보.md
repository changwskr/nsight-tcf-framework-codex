# NSIGHT TCF 테이블 정보 (스키마 · DML)

프로젝트에서 사용하는 **모든 물리 테이블**의 DDL(스키마)과 DML(시드·런타임)을 정리합니다.

| 관점 | 핵심 |
|------|------|
| **공유 DB** | `data/nsight-txlog/nsight_om` (H2 file) — OM·배치·거래로그·세션 |
| **업무 DB** | `h2:mem:nsight_{code}` — EB/EP 업무 테이블 |
| **Gateway DB** | `data/gateway-route` + SESSIONDB(`nsight_om`) |
| **JWT DB** | 별도 H2 또는 OM 사용자 공유 |
| **DDL 원문** | `*/src/main/resources/schema.sql`, Java `*SchemaInitializer` |

관련: [docs/architecture/19-tcf-table.md](../docs/architecture/19-tcf-table.md)

---

## 1. DB 토폴로지

```text
./data/nsight-txlog/nsight_om (H2 file, AUTO_SERVER)
  ├─ TCF_*          프레임워크 (거래로그·거래통제·Timeout)
  ├─ OM_*           운영·대시보드·배치·감사
  ├─ UD_*           파일 메타
  └─ SPRING_SESSION*  OM 로그인 세션

./data/gateway-route (H2 file)
  └─ TCF_GATEWAY_ROUTE

SESSIONDB (nsight_om 공유)
  ├─ SPRING_SESSION*
  ├─ TCF_USER_SESSION
  └─ TCF_GATEWAY_TX_LOG

업무 WAR mem DB
  ├─ EB_USER, EB_EVENT        (eb-service)
  └─ EP_USER_EVENT            (ep-service)
```

---

## 2. 테이블 전체 목록 (32개)

| # | 테이블 | 접두 | 소유 DB | DDL 원본 | 주 쓰기 |
|---|--------|------|---------|----------|---------|
| 1 | `TCF_TX_LOG` | TCF | nsight_om | `tcf-om/schema.sql`, `TransactionLogSchemaInitializer` | 모든 WAR ETF |
| 2 | `TCF_TRANSACTION_CONTROL` | TCF | nsight_om | `schema.sql` | OM Admin |
| 3 | `TCF_SERVICE_TIMEOUT_POLICY` | TCF | nsight_om | `TimeoutPolicySeedData` | OM Admin / seed |
| 4 | `OM_SERVICE_CATALOG` | OM | nsight_om | `schema.sql` | OM Admin / `data.sql` |
| 5 | `OM_USER` | OM | nsight_om / jwt | `schema.sql` | OM / JWT |
| 6 | `OM_AUTH_GROUP` | OM | nsight_om / jwt | `schema.sql` | OM / JWT |
| 7 | `OM_MENU` | OM | nsight_om | `schema.sql` | OM Admin |
| 8 | `OM_AUDIT_LOG` | OM | nsight_om | `schema.sql` | ETF 감사 / OM |
| 9 | `OM_AP_STATUS` | OM | nsight_om | `schema.sql`, `BatchDatabaseMigration` | tcf-batch MERGE |
| 10 | `OM_DB_STATUS` | OM | nsight_om | 동일 | tcf-batch MERGE |
| 11 | `OM_SESSION_STATUS` | OM | nsight_om | 동일 | tcf-batch MERGE |
| 12 | `OM_DEPLOY_STATUS` | OM | nsight_om | 동일 | tcf-batch MERGE |
| 13 | `SPRING_SESSION` | Spring | nsight_om | `schema.sql` | Spring Session JDBC |
| 14 | `SPRING_SESSION_ATTRIBUTES` | Spring | nsight_om | `schema.sql` | Spring Session JDBC |
| 15 | `OM_ERROR_CODE` | OM | nsight_om | `schema.sql` | OM Admin MERGE |
| 16 | `OM_BATCH_JOB` | OM | nsight_om | `schema.sql` | `data.sql` seed |
| 17 | `OM_BATCH_HISTORY` | OM | nsight_om | `schema.sql` | tcf-batch / OM INSERT |
| 18 | `OM_SYSTEM_CONFIG` | OM | nsight_om | `schema.sql` | `data.sql` seed |
| 19 | `OM_FILE_DOWNLOAD_LOG` | OM | nsight_om | `schema.sql` | OM UD INSERT |
| 20 | `UD_FILE_META` | UD | nsight_om | `schema.sql` | OM 파일 업로드 |
| 21 | `OM_COMMON_CODE` | OM | nsight_om | `schema.sql` | OM Admin / seed |
| 22 | `OM_MESSAGE_STRUCT` | OM | nsight_om | `schema.sql` | OM 전문구조 |
| 23 | `OM_MESSAGE_FIELD` | OM | nsight_om | `schema.sql` | OM 전문필드 |
| 24 | `OM_FUNCTION_AUTH` | OM | nsight_om | `schema.sql` | OM Admin |
| 25 | `OM_DATA_AUTH` | OM | nsight_om | `schema.sql` | OM Admin |
| 26 | `OM_AUTH_HISTORY` | OM | nsight_om | `schema.sql` | OM 변경 기록 |
| 27 | `OM_CACHE_STATUS` | OM | nsight_om | `schema.sql` | OM Cache 조회 |
| 28 | `OM_DEPLOY_REQUEST` | OM | nsight_om | `schema.sql` | OM 배포 |
| 29 | `OM_DEPLOY_HISTORY` | OM | nsight_om | `schema.sql` | OM 배포 |
| 30 | `TCF_GATEWAY_ROUTE` | GW | gateway-route | `tcf-gateway/schema.sql` | `data.sql` MERGE |
| 31 | `TCF_USER_SESSION` | GW | SESSIONDB | `tcf-gateway/schema.sql` | Gateway sync INSERT |
| 32 | `TCF_GATEWAY_TX_LOG` | GW | SESSIONDB | `tcf-gateway/schema.sql` | Gateway INSERT |
| 33 | `TCF_JWT_TOKEN` | JWT | jwt DB | `tcf-jwt/schema.sql` | JWT 발급 INSERT |
| 34 | `TCF_REFRESH_TOKEN` | JWT | jwt DB | `schema.sql` | JWT refresh |
| 35 | `TCF_TOKEN_DENYLIST` | JWT | jwt DB | `schema.sql` | JWT revoke MERGE |
| 36 | `TCF_JWT_LOGIN_HISTORY` | JWT | jwt DB | `schema.sql` | JWT login INSERT |
| 37 | `TCF_JWT_SECURITY_POLICY` | JWT | jwt DB | `schema.sql` | JWT MERGE |
| 38 | `EB_USER` | EB | nsight_eb mem | `eb-service/schema.sql` | EB Handler INSERT |
| 39 | `EB_EVENT` | EB | nsight_eb mem | `schema.sql` | EB batch INSERT/UPDATE |
| 40 | `EP_USER_EVENT` | EP | nsight_ep mem | `ep-service/schema.sql` | EP Handler INSERT |

---

## 3. DDL — 공유 DB (`tcf-om/schema.sql`)

**경로:** `tcf-om/src/main/resources/schema.sql`  
**적용:** `spring.sql.init` (local), ztomcat H2 TCP

```sql
-- TCF 거래로그
CREATE TABLE IF NOT EXISTS TCF_TX_LOG (
    LOG_ID VARCHAR(64) NOT NULL,
    TX_TIME VARCHAR(40) NOT NULL,
    BUSINESS_CODE VARCHAR(10),
    SERVICE_ID VARCHAR(100),
    TRANSACTION_CODE VARCHAR(50),
    GUID VARCHAR(64) NOT NULL,
    TRACE_ID VARCHAR(64),
    USER_ID VARCHAR(50),
    BRANCH_ID VARCHAR(20),
    RESULT_STATUS VARCHAR(20),
    RESULT_CODE VARCHAR(20),
    ERROR_CODE VARCHAR(50),
    ELAPSED_TIME_MS BIGINT,
    PRIMARY KEY (LOG_ID)
);
CREATE INDEX IF NOT EXISTS IDX_TCF_TX_GUID ON TCF_TX_LOG (GUID, TX_TIME DESC);
CREATE INDEX IF NOT EXISTS IDX_TCF_TX_SVC ON TCF_TX_LOG (SERVICE_ID, TX_TIME DESC);
CREATE INDEX IF NOT EXISTS IDX_TCF_TX_USER ON TCF_TX_LOG (USER_ID, TX_TIME DESC);

-- TCF 거래통제 (7필드 PK)
CREATE TABLE IF NOT EXISTS TCF_TRANSACTION_CONTROL (
    SERVICE_ID VARCHAR(100) NOT NULL,
    TRANSACTION_CODE VARCHAR(50) NOT NULL,
    BUSINESS_CODE VARCHAR(10) NOT NULL,
    SERVICE_NAME VARCHAR(200) NOT NULL,
    USER_ID VARCHAR(50) NOT NULL,
    CHANNEL_ID VARCHAR(30) NOT NULL,
    BRANCH_ID VARCHAR(30) NOT NULL,
    CONTROL_TYPE VARCHAR(20) NOT NULL DEFAULT 'FULL',
    BLOCK_YN CHAR(1) NOT NULL DEFAULT 'Y',
    PRIMARY KEY (SERVICE_ID, TRANSACTION_CODE, BUSINESS_CODE, SERVICE_NAME,
                 USER_ID, CHANNEL_ID, BRANCH_ID)
);

-- OM 서비스 카탈로그
CREATE TABLE IF NOT EXISTS OM_SERVICE_CATALOG (
    CATALOG_ID VARCHAR(64) NOT NULL,
    BUSINESS_CODE VARCHAR(10) NOT NULL,
    SERVICE_ID VARCHAR(100) NOT NULL,
    TRANSACTION_CODE VARCHAR(50) NOT NULL,
    PROCESSING_TYPE VARCHAR(20) NOT NULL,
    HANDLER_CLASS VARCHAR(200),
    AUTH_CODE VARCHAR(50),
    AUDIT_YN CHAR(1) DEFAULT 'N',
    TIMEOUT_SEC INT DEFAULT 5,
    USE_YN CHAR(1) DEFAULT 'Y',
    DESCRIPTION VARCHAR(500),
    PRIMARY KEY (CATALOG_ID)
);
CREATE UNIQUE INDEX IF NOT EXISTS IDX_OM_SVC_ID ON OM_SERVICE_CATALOG (SERVICE_ID);

-- OM 사용자·권한·메뉴
CREATE TABLE IF NOT EXISTS OM_USER (
    USER_ID VARCHAR(50) NOT NULL,
    USER_NAME VARCHAR(100) NOT NULL,
    PASSWORD_HASH VARCHAR(200),
    BRANCH_ID VARCHAR(20),
    AUTH_GROUP_ID VARCHAR(50),
    USE_YN CHAR(1) DEFAULT 'Y',
    LAST_LOGIN_TIME VARCHAR(40),
    PRIMARY KEY (USER_ID)
);

CREATE TABLE IF NOT EXISTS OM_AUTH_GROUP (
    AUTH_GROUP_ID VARCHAR(50) NOT NULL,
    AUTH_GROUP_NAME VARCHAR(100) NOT NULL,
    DESCRIPTION VARCHAR(500),
    USE_YN CHAR(1) DEFAULT 'Y',
    PRIMARY KEY (AUTH_GROUP_ID)
);

CREATE TABLE IF NOT EXISTS OM_MENU (
    MENU_ID VARCHAR(50) NOT NULL,
    MENU_NAME VARCHAR(100) NOT NULL,
    MENU_URL VARCHAR(200),
    PARENT_MENU_ID VARCHAR(50),
    SORT_ORDER INT DEFAULT 0,
    USE_YN CHAR(1) DEFAULT 'Y',
    PRIMARY KEY (MENU_ID)
);

-- 감사·모니터링 스냅샷
CREATE TABLE IF NOT EXISTS OM_AUDIT_LOG (
    AUDIT_ID VARCHAR(64) NOT NULL,
    AUDIT_TIME VARCHAR(40) NOT NULL,
    USER_ID VARCHAR(50),
    BRANCH_ID VARCHAR(20),
    CUSTOMER_NO VARCHAR(50),
    FUNCTION_ID VARCHAR(50),
    FUNCTION_NAME VARCHAR(100),
    INQUIRY_REASON VARCHAR(200),
    RESULT_STATUS VARCHAR(20),
    CLIENT_IP VARCHAR(50),
    PRIMARY KEY (AUDIT_ID)
);

CREATE TABLE IF NOT EXISTS OM_AP_STATUS (
    AP_ID VARCHAR(50) NOT NULL,
    AP_NAME VARCHAR(100),
    HEALTH_STATUS VARCHAR(20),
    CPU_USAGE_PCT DECIMAL(5,2),
    HEAP_USAGE_PCT DECIMAL(5,2),
    THREAD_COUNT INT,
    CHECKED_AT VARCHAR(40),
    PRIMARY KEY (AP_ID)
);

CREATE TABLE IF NOT EXISTS OM_DB_STATUS (
    DB_ID VARCHAR(50) NOT NULL,
    DB_NAME VARCHAR(100),
    HEALTH_STATUS VARCHAR(20),
    POOL_USAGE_PCT DECIMAL(5,2),
    CHECKED_AT VARCHAR(40),
    PRIMARY KEY (DB_ID)
);

CREATE TABLE IF NOT EXISTS OM_SESSION_STATUS (
    SCOPE_ID VARCHAR(50) NOT NULL,
    SCOPE_NAME VARCHAR(100),
    ACTIVE_COUNT INT,
    EXPIRED_COUNT INT,
    TOTAL_COUNT INT,
    UNIQUE_USER_COUNT INT,
    HEALTH_STATUS VARCHAR(20),
    CHECKED_AT VARCHAR(40),
    PRIMARY KEY (SCOPE_ID)
);

CREATE TABLE IF NOT EXISTS OM_DEPLOY_STATUS (
    BUSINESS_CODE VARCHAR(10) NOT NULL,
    WAR_NAME VARCHAR(50),
    WAR_VERSION VARCHAR(50),
    DEPLOYED_AT VARCHAR(40),
    HEALTH_STATUS VARCHAR(20),
    PRIMARY KEY (BUSINESS_CODE)
);

-- Spring Session JDBC
CREATE TABLE IF NOT EXISTS SPRING_SESSION (
    PRIMARY_ID CHAR(36) NOT NULL,
    SESSION_ID CHAR(36) NOT NULL,
    CREATION_TIME BIGINT NOT NULL,
    LAST_ACCESS_TIME BIGINT NOT NULL,
    MAX_INACTIVE_INTERVAL INT NOT NULL,
    EXPIRY_TIME BIGINT NOT NULL,
    PRINCIPAL_NAME VARCHAR(100),
    CONSTRAINT SPRING_SESSION_PK PRIMARY KEY (PRIMARY_ID)
);

CREATE TABLE IF NOT EXISTS SPRING_SESSION_ATTRIBUTES (
    SESSION_PRIMARY_ID CHAR(36) NOT NULL,
    ATTRIBUTE_NAME VARCHAR(200) NOT NULL,
    ATTRIBUTE_BYTES LONGVARBINARY NOT NULL,
    CONSTRAINT SPRING_SESSION_ATTRIBUTES_PK PRIMARY KEY (SESSION_PRIMARY_ID, ATTRIBUTE_NAME),
    CONSTRAINT SPRING_SESSION_ATTRIBUTES_FK FOREIGN KEY (SESSION_PRIMARY_ID)
        REFERENCES SPRING_SESSION(PRIMARY_ID) ON DELETE CASCADE
);

-- 오류·배치·설정
CREATE TABLE IF NOT EXISTS OM_ERROR_CODE (
    ERROR_CODE VARCHAR(50) NOT NULL,
    ERROR_CATEGORY VARCHAR(20) NOT NULL,
    USER_MESSAGE VARCHAR(500),
    OPERATOR_MESSAGE VARCHAR(500),
    ACTION_GUIDE VARCHAR(500),
    NOTIFY_TARGET VARCHAR(100),
    USE_YN CHAR(1) DEFAULT 'Y',
    PRIMARY KEY (ERROR_CODE)
);

CREATE TABLE IF NOT EXISTS OM_BATCH_JOB (
    JOB_ID VARCHAR(50) NOT NULL,
    JOB_NAME VARCHAR(100) NOT NULL,
    BUSINESS_CODE VARCHAR(10),
    CRON_EXPR VARCHAR(50),
    USE_YN CHAR(1) DEFAULT 'Y',
    DESCRIPTION VARCHAR(500),
    PRIMARY KEY (JOB_ID)
);

CREATE TABLE IF NOT EXISTS OM_BATCH_HISTORY (
    HISTORY_ID VARCHAR(64) NOT NULL,
    JOB_ID VARCHAR(50) NOT NULL,
    RUN_TIME VARCHAR(40) NOT NULL,
    RUN_STATUS VARCHAR(20),
    DURATION_MS BIGINT,
    RESULT_MESSAGE VARCHAR(500),
    PRIMARY KEY (HISTORY_ID)
);

CREATE TABLE IF NOT EXISTS OM_SYSTEM_CONFIG (
    CONFIG_KEY VARCHAR(100) NOT NULL,
    CONFIG_CATEGORY VARCHAR(30) NOT NULL,
    CONFIG_VALUE VARCHAR(500),
    EDITABLE_YN CHAR(1) DEFAULT 'N',
    DESCRIPTION VARCHAR(500),
    PRIMARY KEY (CONFIG_KEY)
);

CREATE TABLE IF NOT EXISTS OM_FILE_DOWNLOAD_LOG (
    LOG_ID VARCHAR(64) NOT NULL,
    DOWNLOAD_TIME VARCHAR(40) NOT NULL,
    USER_ID VARCHAR(50),
    FILE_NAME VARCHAR(200),
    FILE_SIZE BIGINT,
    BUSINESS_CODE VARCHAR(10),
    RESULT_STATUS VARCHAR(20),
    CLIENT_IP VARCHAR(50),
    PRIMARY KEY (LOG_ID)
);

-- 파일·공통코드·전문구조
CREATE TABLE IF NOT EXISTS UD_FILE_META (
    FILE_ID VARCHAR(36) NOT NULL,
    ORIGINAL_NAME VARCHAR(255) NOT NULL,
    CONTENT_TYPE VARCHAR(100),
    FILE_SIZE BIGINT NOT NULL,
    DESCRIPTION VARCHAR(500),
    UPLOAD_USER VARCHAR(50),
    UPLOAD_TIME VARCHAR(40) NOT NULL,
    BUSINESS_CODE VARCHAR(10) DEFAULT 'UD',
    USE_YN CHAR(1) DEFAULT 'Y',
    PRIMARY KEY (FILE_ID)
);

CREATE TABLE IF NOT EXISTS OM_COMMON_CODE (
    CODE_GROUP VARCHAR(50) NOT NULL,
    CODE VARCHAR(50) NOT NULL,
    CODE_NAME VARCHAR(100) NOT NULL,
    SORT_ORDER INT DEFAULT 0,
    USE_YN CHAR(1) DEFAULT 'Y',
    DESCRIPTION VARCHAR(500),
    CREATED_AT VARCHAR(40),
    UPDATED_AT VARCHAR(40),
    PRIMARY KEY (CODE_GROUP, CODE)
);

CREATE TABLE IF NOT EXISTS OM_MESSAGE_STRUCT (
    STRUCT_ID VARCHAR(64) NOT NULL,
    STRUCT_CODE VARCHAR(80) NOT NULL,
    BUSINESS_CODE VARCHAR(10),
    SERVICE_ID VARCHAR(100),
    TRANSACTION_CODE VARCHAR(50),
    MESSAGE_TYPE VARCHAR(20) NOT NULL,
    SEGMENT_TYPE VARCHAR(20) NOT NULL,
    STRUCT_NAME VARCHAR(200) NOT NULL,
    DESCRIPTION VARCHAR(500),
    SAMPLE_JSON CLOB,
    USE_YN CHAR(1) DEFAULT 'Y',
    PRIMARY KEY (STRUCT_ID)
);

CREATE TABLE IF NOT EXISTS OM_MESSAGE_FIELD (
    FIELD_ID VARCHAR(64) NOT NULL,
    STRUCT_ID VARCHAR(64) NOT NULL,
    FIELD_KEY VARCHAR(100) NOT NULL,
    FIELD_LABEL VARCHAR(200),
    DATA_TYPE VARCHAR(20) NOT NULL DEFAULT 'STRING',
    REQUIRED_YN CHAR(1) DEFAULT 'N',
    MAX_LENGTH INT,
    DEFAULT_VALUE VARCHAR(500),
    SAMPLE_VALUE VARCHAR(500),
    VALIDATION_RULE VARCHAR(200),
    DESCRIPTION VARCHAR(500),
    SORT_ORDER INT DEFAULT 0,
    PRIMARY KEY (FIELD_ID)
);

-- 권한·캐시·배포
CREATE TABLE IF NOT EXISTS OM_FUNCTION_AUTH (
    AUTH_ID VARCHAR(64) NOT NULL,
    AUTH_GROUP_ID VARCHAR(50) NOT NULL,
    MENU_ID VARCHAR(50),
    CAN_INQUIRY CHAR(1) DEFAULT 'N',
    CAN_REGISTER CHAR(1) DEFAULT 'N',
    CAN_UPDATE CHAR(1) DEFAULT 'N',
    CAN_DELETE CHAR(1) DEFAULT 'N',
    CAN_DOWNLOAD CHAR(1) DEFAULT 'N',
    PRIMARY KEY (AUTH_ID)
);

CREATE TABLE IF NOT EXISTS OM_DATA_AUTH (
    AUTH_ID VARCHAR(64) NOT NULL,
    AUTH_GROUP_ID VARCHAR(50) NOT NULL,
    BRANCH_SCOPE VARCHAR(50),
    CUSTOMER_ACCESS_LEVEL VARCHAR(50),
    DESCRIPTION VARCHAR(500),
    PRIMARY KEY (AUTH_ID)
);

CREATE TABLE IF NOT EXISTS OM_AUTH_HISTORY (
    HISTORY_ID VARCHAR(64) NOT NULL,
    CHANGED_AT VARCHAR(40) NOT NULL,
    CHANGED_BY VARCHAR(50),
    TARGET_TYPE VARCHAR(50),
    TARGET_ID VARCHAR(100),
    BEFORE_VALUE VARCHAR(1000),
    AFTER_VALUE VARCHAR(1000),
    CHANGE_REASON VARCHAR(500),
    PRIMARY KEY (HISTORY_ID)
);

CREATE TABLE IF NOT EXISTS OM_CACHE_STATUS (
    CACHE_NAME VARCHAR(100) NOT NULL,
    CACHE_KEY VARCHAR(200) NOT NULL DEFAULT '*',
    ENTRY_COUNT INT DEFAULT 0,
    LAST_UPDATED VARCHAR(40),
    TTL_SEC INT,
    PRIMARY KEY (CACHE_NAME, CACHE_KEY)
);

CREATE TABLE IF NOT EXISTS OM_DEPLOY_REQUEST (
    DEPLOY_REQUEST_ID VARCHAR(40) NOT NULL,
    REQUEST_TYPE VARCHAR(20) NOT NULL,
    ENV_CODE VARCHAR(20),
    BUSINESS_CODE VARCHAR(20),
    MODULE_NAME VARCHAR(100),
    BRANCH_NAME VARCHAR(100),
    COMMIT_ID VARCHAR(100),
    GRADLE_TASK VARCHAR(200),
    ARTIFACT_NAME VARCHAR(200),
    STATUS VARCHAR(30) NOT NULL,
    REQUEST_USER_ID VARCHAR(50),
    APPROVE_USER_ID VARCHAR(50),
    REQUEST_TIME VARCHAR(40) NOT NULL,
    APPROVE_TIME VARCHAR(40),
    START_TIME VARCHAR(40),
    END_TIME VARCHAR(40),
    ERROR_MESSAGE CLOB,
    PRIMARY KEY (DEPLOY_REQUEST_ID)
);

CREATE TABLE IF NOT EXISTS OM_DEPLOY_HISTORY (
    DEPLOY_HISTORY_ID VARCHAR(40) NOT NULL,
    DEPLOY_REQUEST_ID VARCHAR(40),
    ENV_CODE VARCHAR(20),
    TARGET_SERVER VARCHAR(100),
    CONTEXT_PATH VARCHAR(50),
    BEFORE_VERSION VARCHAR(100),
    AFTER_VERSION VARCHAR(100),
    RESULT_CODE VARCHAR(20),
    RESULT_MESSAGE VARCHAR(1000),
    HEALTH_CHECK_URL VARCHAR(500),
    HEALTH_CHECK_RESULT VARCHAR(20),
    CREATED_TIME VARCHAR(40) NOT NULL,
    PRIMARY KEY (DEPLOY_HISTORY_ID)
);
```

### 3.1 `TCF_SERVICE_TIMEOUT_POLICY` (Java DDL)

**경로:** `tcf-om/.../TimeoutPolicySeedData.ensureTable()`

```sql
CREATE TABLE IF NOT EXISTS TCF_SERVICE_TIMEOUT_POLICY (
    SERVICE_ID VARCHAR(100) NOT NULL,
    TRANSACTION_CODE VARCHAR(50) NOT NULL,
    BUSINESS_CODE VARCHAR(10) NOT NULL,
    SERVICE_NAME VARCHAR(200) NOT NULL,
    ONLINE_TIMEOUT_SEC INT NOT NULL DEFAULT 5,
    TX_TIMEOUT_SEC INT NOT NULL DEFAULT 5,
    DB_QUERY_TIMEOUT_SEC INT NOT NULL DEFAULT 3,
    EXTERNAL_CONNECT_TIMEOUT_MS INT NOT NULL DEFAULT 3000,
    EXTERNAL_READ_TIMEOUT_MS INT NOT NULL DEFAULT 5000,
    TIMEOUT_ACTION VARCHAR(30) NOT NULL DEFAULT 'FAIL',
    USE_YN CHAR(1) NOT NULL DEFAULT 'Y',
    DESCRIPTION VARCHAR(500),
    CREATED_AT TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UPDATED_AT TIMESTAMP,
    PRIMARY KEY (SERVICE_ID, TRANSACTION_CODE, BUSINESS_CODE)
);
```

---

## 4. DDL — Gateway (`tcf-gateway/schema.sql`)

```sql
CREATE TABLE IF NOT EXISTS TCF_GATEWAY_ROUTE (
    ROUTE_ID VARCHAR(60) NOT NULL,
    ENV_CODE VARCHAR(20) NOT NULL,
    ROUTE_GROUP_CODE VARCHAR(30) NOT NULL,
    ROUTE_GROUP_NAME VARCHAR(100) NOT NULL,
    BUSINESS_CODE VARCHAR(10) NOT NULL,
    BUSINESS_NAME VARCHAR(100) NOT NULL,
    TARGET_BASE_URL VARCHAR(300) NOT NULL,
    CONTEXT_PATH VARCHAR(100) NOT NULL,
    ONLINE_PATH VARCHAR(100) DEFAULT '/online' NOT NULL,
    HEALTH_CHECK_PATH VARCHAR(200),
    CONNECT_TIMEOUT_MS INT DEFAULT 3000 NOT NULL,
    READ_TIMEOUT_MS INT DEFAULT 5000 NOT NULL,
    USE_YN CHAR(1) DEFAULT 'Y' NOT NULL,
    SORT_ORDER INT,
    DESCRIPTION VARCHAR(500),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP,
    CONSTRAINT PK_TCF_GATEWAY_ROUTE PRIMARY KEY (ROUTE_ID),
    CONSTRAINT UK_TCF_GATEWAY_ROUTE_01 UNIQUE (ENV_CODE, BUSINESS_CODE)
);

CREATE TABLE IF NOT EXISTS TCF_USER_SESSION (
    SESSION_ID VARCHAR(100) NOT NULL,
    USER_ID VARCHAR(50) NOT NULL,
    USER_NAME VARCHAR(100),
    BRANCH_ID VARCHAR(20),
    CHANNEL_ID VARCHAR(20),
    AUTH_GROUP_ID VARCHAR(50),
    SESSION_TYPE VARCHAR(20) DEFAULT 'BUSINESS' NOT NULL,
    LOGIN_TIME TIMESTAMP NOT NULL,
    LAST_ACCESS_TIME TIMESTAMP NOT NULL,
    ABSOLUTE_EXPIRE_TIME TIMESTAMP NOT NULL,
    CLIENT_IP VARCHAR(50),
    USER_AGENT VARCHAR(500),
    CENTER_ID VARCHAR(20),
    WAS_ID VARCHAR(50),
    STATUS VARCHAR(20) DEFAULT 'ACTIVE' NOT NULL,
    LOGOUT_TIME TIMESTAMP,
    LOGOUT_REASON VARCHAR(200),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP,
    CONSTRAINT PK_TCF_USER_SESSION PRIMARY KEY (SESSION_ID)
);

CREATE TABLE IF NOT EXISTS TCF_GATEWAY_TX_LOG (
    LOG_ID VARCHAR(64) NOT NULL,
    TX_TIME VARCHAR(40) NOT NULL,
    ENV_CODE VARCHAR(20),
    BUSINESS_CODE VARCHAR(10),
    SERVICE_ID VARCHAR(100),
    TRANSACTION_CODE VARCHAR(50),
    GUID VARCHAR(64),
    TRACE_ID VARCHAR(64),
    USER_ID VARCHAR(50),
    BRANCH_ID VARCHAR(20),
    SESSION_ID VARCHAR(100),
    TARGET_URL VARCHAR(500),
    HTTP_STATUS INT,
    RESULT_STATUS VARCHAR(20),
    RESULT_CODE VARCHAR(20),
    ERROR_CODE VARCHAR(50),
    ELAPSED_TIME_MS BIGINT,
    PHASE VARCHAR(50),
    CONSTRAINT PK_TCF_GATEWAY_TX_LOG PRIMARY KEY (LOG_ID)
);
```

---

## 5. DDL — JWT (`tcf-jwt/schema.sql`)

```sql
CREATE TABLE IF NOT EXISTS TCF_JWT_TOKEN (
    TOKEN_ID VARCHAR(64) NOT NULL,
    JTI VARCHAR(100) NOT NULL,
    ISSUER VARCHAR(100) NOT NULL,
    USER_ID VARCHAR(50) NOT NULL,
    BRANCH_ID VARCHAR(30),
    CHANNEL_ID VARCHAR(30),
    AUTH_GROUP_ID VARCHAR(50),
    TOKEN_TYPE VARCHAR(20) NOT NULL,
    AUDIENCE VARCHAR(100),
    ISSUED_AT TIMESTAMP NOT NULL,
    EXPIRES_AT TIMESTAMP NOT NULL,
    REVOKED_YN CHAR(1) DEFAULT 'N',
    REVOKED_AT TIMESTAMP,
    REVOKE_REASON VARCHAR(200),
    CLIENT_IP VARCHAR(50),
    USER_AGENT VARCHAR(500),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (TOKEN_ID),
    CONSTRAINT TCF_JWT_TOKEN_UK1 UNIQUE (ISSUER, JTI)
);

CREATE TABLE IF NOT EXISTS TCF_REFRESH_TOKEN (
    REFRESH_TOKEN_ID VARCHAR(64) NOT NULL,
    USER_ID VARCHAR(50) NOT NULL,
    TOKEN_HASH VARCHAR(256) NOT NULL,
    TOKEN_FAMILY_ID VARCHAR(64),
    ISSUED_AT TIMESTAMP NOT NULL,
    EXPIRES_AT TIMESTAMP NOT NULL,
    LAST_USED_AT TIMESTAMP,
    ROTATED_YN CHAR(1) DEFAULT 'N',
    REVOKED_YN CHAR(1) DEFAULT 'N',
    REVOKED_AT TIMESTAMP,
    CLIENT_IP VARCHAR(50),
    USER_AGENT VARCHAR(500),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (REFRESH_TOKEN_ID)
);

CREATE TABLE IF NOT EXISTS TCF_TOKEN_DENYLIST (
    ISSUER VARCHAR(100) NOT NULL,
    JTI VARCHAR(100) NOT NULL,
    USER_ID VARCHAR(50),
    EXPIRES_AT TIMESTAMP NOT NULL,
    REVOKED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    REVOKE_REASON VARCHAR(200),
    PRIMARY KEY (ISSUER, JTI)
);

CREATE TABLE IF NOT EXISTS TCF_JWT_LOGIN_HISTORY (
    LOG_ID VARCHAR(64) NOT NULL,
    USER_ID VARCHAR(50) NOT NULL,
    LOGIN_RESULT VARCHAR(20) NOT NULL,
    FAIL_REASON VARCHAR(500),
    CHANNEL_ID VARCHAR(30),
    CLIENT_IP VARCHAR(50),
    LOGIN_TIME TIMESTAMP NOT NULL,
    PRIMARY KEY (LOG_ID)
);

CREATE TABLE IF NOT EXISTS TCF_JWT_SECURITY_POLICY (
    POLICY_ID VARCHAR(20) NOT NULL,
    ISSUER VARCHAR(100) NOT NULL,
    AUDIENCE VARCHAR(100) NOT NULL,
    ACCESS_TOKEN_VALID_MINUTES INT NOT NULL,
    REFRESH_TOKEN_VALID_HOURS INT NOT NULL,
    ALGORITHM VARCHAR(20) NOT NULL,
    CLOCK_SKEW_SECONDS INT NOT NULL,
    DENYLIST_CHECK_ENABLED CHAR(1) DEFAULT 'Y',
    REFRESH_TOKEN_ROTATION_ENABLED CHAR(1) DEFAULT 'Y',
    UPDATED_AT TIMESTAMP,
    UPDATED_BY VARCHAR(50),
    PRIMARY KEY (POLICY_ID)
);
```

(`OM_USER`, `OM_AUTH_GROUP` — `tcf-om/schema.sql`과 동일)

---

## 6. DDL — 업무 (`eb-service`, `ep-service`)

```sql
-- eb-service/src/main/resources/schema.sql
CREATE TABLE IF NOT EXISTS EB_USER (
    USER_ID     VARCHAR(50) PRIMARY KEY,
    USER_NAME   VARCHAR(100),
    BRANCH_ID   VARCHAR(20),
    CREATED_AT  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS EB_EVENT (
    EVENT_ID      VARCHAR(50) PRIMARY KEY,
    USER_ID       VARCHAR(50) NOT NULL,
    EVENT_TYPE    VARCHAR(30) NOT NULL,
    EVENT_STATUS  VARCHAR(20) NOT NULL,
    RETRY_COUNT   INT DEFAULT 0,
    CREATED_AT    TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    SENT_AT       TIMESTAMP
);
CREATE INDEX IF NOT EXISTS IX_EB_EVENT_STATUS ON EB_EVENT (EVENT_STATUS, CREATED_AT);

-- ep-service/src/main/resources/schema.sql
CREATE TABLE IF NOT EXISTS EP_USER_EVENT (
    EVENT_ID     VARCHAR(50) PRIMARY KEY,
    USER_ID      VARCHAR(50) NOT NULL,
    EVENT_TYPE   VARCHAR(30) NOT NULL,
    RECEIVED_AT  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 7. DML — 시드 데이터 (`data.sql`)

| 파일 | 대상 테이블 |
|------|-------------|
| `tcf-om/src/main/resources/data.sql` | `OM_AUTH_GROUP`, `OM_USER`, `OM_MENU`, `OM_SERVICE_CATALOG`, `OM_ERROR_CODE`, `OM_BATCH_JOB`, `OM_BATCH_HISTORY`, `OM_SYSTEM_CONFIG`, `OM_COMMON_CODE`, `OM_FUNCTION_AUTH`, `OM_DATA_AUTH`, `OM_MESSAGE_STRUCT`, `OM_MESSAGE_FIELD` |
| `tcf-gateway/src/main/resources/data.sql` | `TCF_GATEWAY_ROUTE` (MERGE LOCAL/DEV/PROD) |
| `tcf-jwt/src/main/resources/data.sql` | `OM_AUTH_GROUP`, `OM_USER` |
| `tcf-om/scripts/seed-common-code.sql` | `OM_COMMON_CODE` (MERGE 다건) |

### 7.1 OM 시드 예시 (`data.sql`)

```sql
INSERT INTO OM_AUTH_GROUP (AUTH_GROUP_ID, AUTH_GROUP_NAME, DESCRIPTION, USE_YN) VALUES
('ROLE_ADMIN', '시스템관리자', 'OM 전체 관리', 'Y');

INSERT INTO OM_USER (USER_ID, USER_NAME, PASSWORD_HASH, BRANCH_ID, AUTH_GROUP_ID, USE_YN, LAST_LOGIN_TIME) VALUES
('admin01', '운영관리자', NULL, '000001', 'ROLE_ADMIN', 'Y', '2026-06-14T09:15:00+09:00');

INSERT INTO OM_SERVICE_CATALOG (CATALOG_ID, BUSINESS_CODE, SERVICE_ID, TRANSACTION_CODE, ...) VALUES
('CAT-001', 'SV', 'SV.Sample.inquiry', 'SV-INQ-0001', ...);

INSERT INTO OM_BATCH_JOB (JOB_ID, JOB_NAME, BUSINESS_CODE, CRON_EXPR, USE_YN, DESCRIPTION) VALUES
('BAT-BATCH-001', 'AP 상태 수집', 'BATCH', '0 */5 * * * *', 'Y', '...'),
('BAT-OM-002', '만료 세션 정리', 'OM', NULL, 'Y', '...');
```

### 7.2 Gateway 라우트 시드 (MERGE)

```sql
MERGE INTO TCF_GATEWAY_ROUTE KEY(ROUTE_ID) VALUES
('LOCAL-GW-SV', 'LOCAL', 'MSA-B', '고객/마케팅 조회 그룹', 'SV', 'Single View',
 'http://127.0.0.1:8086', '/sv', '/online', '/sv/actuator/health',
 3000, 5000, 'Y', 60, 'LOCAL SV bootRun :8086', CURRENT_TIMESTAMP, NULL);
```

### 7.3 런타임 MERGE 시드 (Java)

| 클래스 | 테이블 | DML |
|--------|--------|-----|
| `TimeoutPolicySeedData` | `TCF_SERVICE_TIMEOUT_POLICY` | 카탈로그 기준 MERGE |
| `ServiceCatalogSeedData` | `OM_SERVICE_CATALOG` | MERGE |
| `FunctionAuthSeedData` | `OM_FUNCTION_AUTH` | MERGE |
| `OmDatabaseMigration` | 다수 | MERGE·DDL 보강 |

```sql
-- TimeoutPolicySeedData.mergeAll() 요약
MERGE INTO TCF_SERVICE_TIMEOUT_POLICY (...)
KEY (SERVICE_ID, TRANSACTION_CODE, BUSINESS_CODE)
SELECT ... FROM OM_SERVICE_CATALOG c WHERE USE_YN = 'Y';
```

---

## 8. DML — 런타임 (프레임워크)

### 8.1 `TCF_TX_LOG` INSERT

**파일:** `JdbcTransactionLogRepository.java`  
**시점:** ETF `TX_END` (업무 TX와 **별도 autocommit**)

```sql
INSERT INTO TCF_TX_LOG (
    LOG_ID, TX_TIME, BUSINESS_CODE, SERVICE_ID, TRANSACTION_CODE,
    GUID, TRACE_ID, USER_ID, BRANCH_ID,
    RESULT_STATUS, RESULT_CODE, ERROR_CODE, ELAPSED_TIME_MS
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?);
```

### 8.2 대시보드 MERGE (`tcf-batch`)

**파일:** `OmDashboardStatusRepository.java`

```sql
MERGE INTO OM_AP_STATUS (AP_ID, AP_NAME, HEALTH_STATUS, CPU_USAGE_PCT, HEAP_USAGE_PCT,
                         THREAD_COUNT, CHECKED_AT)
KEY (AP_ID) VALUES (?, ?, ?, ?, ?, ?, ?);

MERGE INTO OM_DB_STATUS (DB_ID, DB_NAME, HEALTH_STATUS, POOL_USAGE_PCT, CHECKED_AT)
KEY (DB_ID) VALUES (?, ?, ?, ?, ?);

MERGE INTO OM_SESSION_STATUS (SCOPE_ID, SCOPE_NAME, ACTIVE_COUNT, EXPIRED_COUNT,
                              TOTAL_COUNT, UNIQUE_USER_COUNT, HEALTH_STATUS, CHECKED_AT)
KEY (SCOPE_ID) VALUES (?, ?, ?, ?, ?, ?, ?, ?);

MERGE INTO OM_DEPLOY_STATUS (BUSINESS_CODE, WAR_NAME, WAR_VERSION, DEPLOYED_AT, HEALTH_STATUS)
KEY (BUSINESS_CODE) VALUES (?, ?, ?, ?, ?);

INSERT INTO OM_BATCH_HISTORY (HISTORY_ID, JOB_ID, RUN_TIME, RUN_STATUS, DURATION_MS, RESULT_MESSAGE)
VALUES (?, ?, ?, ?, ?, ?);
```

### 8.3 세션 정리 DELETE

**파일:** `OmOperationMapper.xml` / `OmSessionCleanupService`

```sql
SELECT COUNT(*) FROM SPRING_SESSION WHERE EXPIRY_TIME <= ?;
DELETE FROM SPRING_SESSION WHERE EXPIRY_TIME <= ?;
DELETE FROM SPRING_SESSION WHERE SESSION_ID = ?;
```

### 8.4 Gateway

```sql
-- UserSessionDao.sync
INSERT INTO TCF_USER_SESSION (SESSION_ID, USER_ID, USER_NAME, ...) VALUES (...);

-- GatewayTransactionLogDao
INSERT INTO TCF_GATEWAY_TX_LOG (LOG_ID, TX_TIME, ...) VALUES (...);
```

---

## 9. DML — OM Admin (`OmOperationMapper.xml`)

| 테이블 | INSERT | UPDATE | DELETE | MERGE |
|--------|--------|--------|--------|-------|
| `OM_SERVICE_CATALOG` | ● | ● | ● | — |
| `OM_USER` | ● | ● | ● | — |
| `OM_MENU` | ● | ● | ● | — |
| `OM_AUTH_GROUP` | ● | ● | ● | — |
| `TCF_TRANSACTION_CONTROL` | ● | ● | ● | — |
| `TCF_SERVICE_TIMEOUT_POLICY` | ● | ● | ● | — |
| `OM_ERROR_CODE` | ● | ● | ● | ● |
| `OM_COMMON_CODE` | ● | ● | ● | — |
| `OM_FUNCTION_AUTH` | ● | ● | ● | — |
| `OM_DATA_AUTH` | ● | ● | ● | — |
| `OM_AUTH_HISTORY` | ● | — | `deleteAll` | — |
| `OM_AUDIT_LOG` | ● | — | `deleteAll` | — |
| `OM_BATCH_HISTORY` | ● | — | `deleteAll` | — |
| `OM_MESSAGE_STRUCT` | ● | ● | ● | — |
| `OM_MESSAGE_FIELD` | ● | ● | ● | — |
| `OM_FILE_DOWNLOAD_LOG` | ● | — | — | — |
| `TCF_TX_LOG` | — | — | `deleteAll` | — |
| `OM_*_STATUS` | — | — | `deleteAll` | — |
| `OM_DEPLOY_*` | ● (`OmDeployMapper`) | ● | — | — |

### 9.1 거래통제 CRUD 예시

```sql
INSERT INTO TCF_TRANSACTION_CONTROL (
    SERVICE_ID, TRANSACTION_CODE, BUSINESS_CODE, SERVICE_NAME,
    USER_ID, CHANNEL_ID, BRANCH_ID, CONTROL_TYPE, BLOCK_YN
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?);

UPDATE TCF_TRANSACTION_CONTROL
   SET CONTROL_TYPE = ?, BLOCK_YN = ?
 WHERE SERVICE_ID = ? AND TRANSACTION_CODE = ? AND BUSINESS_CODE = ?
   AND SERVICE_NAME = ? AND USER_ID = ? AND CHANNEL_ID = ? AND BRANCH_ID = ?;

DELETE FROM TCF_TRANSACTION_CONTROL WHERE ... (7필드 PK);
```

### 9.2 오류코드 MERGE

```sql
MERGE INTO OM_ERROR_CODE (ERROR_CODE, ERROR_CATEGORY, USER_MESSAGE, OPERATOR_MESSAGE,
                          ACTION_GUIDE, NOTIFY_TARGET, USE_YN)
KEY (ERROR_CODE) VALUES (?, ?, ?, ?, ?, ?, ?);
```

---

## 10. DML — JWT (`JwtTokenMapper.xml`, `JwtAdminMapper.xml`)

```sql
INSERT INTO TCF_JWT_TOKEN (TOKEN_ID, JTI, ISSUER, USER_ID, ...) VALUES (...);
UPDATE TCF_JWT_TOKEN SET REVOKED_YN = 'Y', REVOKED_AT = ?, REVOKE_REASON = ? WHERE ...;

INSERT INTO TCF_REFRESH_TOKEN (...) VALUES (...);
UPDATE TCF_REFRESH_TOKEN SET ROTATED_YN = 'Y', REVOKED_YN = 'Y' WHERE ...;

MERGE INTO TCF_TOKEN_DENYLIST (ISSUER, JTI, USER_ID, EXPIRES_AT, REVOKED_AT, REVOKE_REASON)
KEY (ISSUER, JTI) VALUES (?, ?, ?, ?, ?, ?);

INSERT INTO TCF_JWT_LOGIN_HISTORY (LOG_ID, USER_ID, LOGIN_RESULT, ...) VALUES (...);

MERGE INTO TCF_JWT_SECURITY_POLICY (...) KEY (POLICY_ID) VALUES (...);

UPDATE OM_USER SET PASSWORD_HASH = ?, LAST_LOGIN_TIME = ? WHERE USER_ID = ?;
```

---

## 11. DML — EB / EP 업무

```sql
-- EbUserMapper
INSERT INTO EB_USER (USER_ID, USER_NAME, BRANCH_ID) VALUES (?, ?, ?);

-- EbEventMapper
INSERT INTO EB_EVENT (EVENT_ID, USER_ID, EVENT_TYPE, EVENT_STATUS, RETRY_COUNT)
VALUES (?, ?, ?, ?, ?);

UPDATE EB_EVENT SET EVENT_STATUS = ?, SENT_AT = CASE WHEN ? = 'SENT' THEN CURRENT_TIMESTAMP ELSE SENT_AT END
 WHERE EVENT_ID = ?;

SELECT ... FROM EB_EVENT WHERE EVENT_STATUS = 'READY' ORDER BY CREATED_AT FETCH FIRST ? ROWS ONLY;

-- EpUserEventMapper
INSERT INTO EP_USER_EVENT (EVENT_ID, USER_ID, EVENT_TYPE) VALUES (?, ?, ?);
```

---

## 12. DDL/DML 파일 인덱스

| 유형 | 경로 |
|------|------|
| OM DDL | `tcf-om/src/main/resources/schema.sql` |
| OM 시드 | `tcf-om/src/main/resources/data.sql` |
| OM DML | `tcf-om/src/main/resources/mapper/om/OmOperationMapper.xml` |
| OM 배포 DML | `tcf-om/src/main/resources/mapper/om/OmDeployMapper.xml` |
| Timeout DDL+MERGE | `tcf-om/.../TimeoutPolicySeedData.java` |
| 거래로그 DDL | `tcf-web/.../TransactionLogSchemaInitializer.java` |
| 거래로그 INSERT | `tcf-web/.../JdbcTransactionLogRepository.java` |
| Batch DDL | `tcf-batch/.../BatchDatabaseMigration.java` |
| Batch MERGE | `tcf-batch/.../OmDashboardStatusRepository.java` |
| Gateway DDL | `tcf-gateway/src/main/resources/schema.sql` |
| Gateway 시드 | `tcf-gateway/src/main/resources/data.sql` |
| Gateway Oracle | `tcf-gateway/sql/oracle/*.sql` |
| JWT DDL | `tcf-jwt/src/main/resources/schema.sql` |
| JWT DML | `tcf-jwt/src/main/resources/mapper/jwt/*.xml` |
| EB DDL/DML | `eb-service/src/main/resources/schema.sql`, `mapper/eb/*.xml` |
| EP DDL/DML | `ep-service/src/main/resources/schema.sql`, `mapper/ep/*.xml` |
| 공통코드 시드 | `tcf-om/scripts/seed-common-code.sql` |

---

## 13. 모듈별 R/W 요약

| 모듈 | 읽기 | 쓰기 |
|------|------|------|
| 업무 WAR | mem (샘플 SELECT) | `TCF_TX_LOG` INSERT |
| tcf-om | OM 전 테이블 | OM 전 테이블 CRUD |
| tcf-batch | `SPRING_SESSION`, 상태 테이블 | `OM_*_STATUS` MERGE, `OM_BATCH_HISTORY` |
| tcf-gateway | `TCF_GATEWAY_ROUTE`, SESSIONDB | `TCF_USER_SESSION`, `TCF_GATEWAY_TX_LOG` |
| tcf-jwt | JWT 테이블, `OM_USER` | JWT 토큰·이력 |
| eb-service | `EB_*` | `EB_USER`, `EB_EVENT` |
| ep-service | `EP_USER_EVENT` | `EP_USER_EVENT` |

---

## 14. 개발자 체크리스트

- [ ] 신규 테이블은 `schema.sql` + 해당 Mapper XML에 DML 정의?
- [ ] 공유 DB 테이블은 `tcf-om/schema.sql` 또는 `OmDatabaseMigration`에 DDL?
- [ ] 시드는 `data.sql` 또는 `*SeedData.java` MERGE?
- [ ] 대시보드 스냅샷은 `MERGE` (idempotent) 패턴?
- [ ] `TCF_TX_LOG`는 업무 TX와 별도 DS·autocommit?
- [ ] 운영 Oracle 전환 시 `tcf-gateway/sql/oracle/` 참고?

---

## 15. 관련 문서

| 문서 | 내용 |
|------|------|
| [docs/architecture/19-tcf-table.md](../docs/architecture/19-tcf-table.md) | 테이블 아키텍처 |
| [docs/architecture/09-transaction log.md](../docs/architecture/09-transaction%20log.md) | 거래로그 |
| [DAO처리.md](DAO처리.md) | Mapper·페이징 |
| [배치관리.md](배치관리.md) | OM_*_STATUS 수집 |
| [세션관리.md](세션관리.md) | SPRING_SESSION |
| [솔루션환경구성.md](솔루션환경구성.md) | DataSource |
