<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nh.nsight.singleview.dao.SingleViewMapper">
  <select id="selectSingleView" parameterType="map" resultType="map" timeout="3" fetchSize="100">
    SELECT CUSTOMER_NO, CUSTOMER_NAME, BRANCH_ID, CUSTOMER_GRADE
      FROM RDW_CUSTOMER_SINGLE_VIEW
     WHERE CUSTOMER_NO = #{customerNo}
  </select>
</mapper>
