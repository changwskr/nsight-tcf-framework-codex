# 10장. 세션 관리 구조 — 설명

## 설계서 절 목차

10.1~10.5 개요·생성·검증 · 10.6~10.8 컴포넌트·테이블 · 10.9 Timeout · 10.10~10.12 Gateway·Sticky·다중WAR · 10.13~10.14 설정·검증 · 10.15~10.17 중복로그인·상태·오류 · 10.18~10.21 OM·운영·장애

---

## 핵심 결론

> 세션 원천 = **SESSIONDB** (Spring Session JDBC). WAS 메모리 ❌.

---

## 세션 아키텍처 (10.3)

```
브라우저 (JSESSIONID/NSIGHTSID)
→ Apache → [Gateway] → 업무 WAR
→ Spring Session JDBC Filter
→ STF.preProcess() — SessionValidator: header.userId 필수 (설정 시)
→ SESSIONDB: SPRING_SESSION, SPRING_SESSION_ATTRIBUTES, TCF_USER_SESSION
```

## 세션 생성 (10.4)

1. `OM.Auth.login` / `OM.Auth.ssoLogin` (tcf-om)  
2. OM_USER·권한 조회  
3. HttpSession → Spring Session JDBC 저장  
4. Cookie 응답  

## 업무 요청 검증 (10.5)

```
POST /sv/online + Cookie + header(userId, branchId, channelId)
→ Spring Session Filter: SPRING_SESSION (WAR)
→ STF SessionValidator: header.userId 존재 여부
→ 권한·거래통제·Timeout (STF/BTF)
→ Dispatcher
```

Gateway 4단계·JWT는 tcf-gateway GSF — [09-Gateway라우팅.md](./09-Gateway라우팅.md)

## 테이블 (10.7)

| 테이블 | 용도 |
|--------|------|
| SPRING_SESSION | Spring Session 기본 |
| SPRING_SESSION_ATTRIBUTES | 속성 Blob |
| TCF_USER_SESSION | TCF/Gateway 확장 (지점·채널·상태) |

## Timeout (10.9)

| 유형 | 설명 |
|------|------|
| Idle | 미사용 시간 |
| Absolute | 로그인 후 최대 시간 |

## Gateway·Sticky (10.10~10.11)

- Gateway: Cookie **전달** (Sticky는 Apache/L4)
- DeltaManager(WAS 메모리) ❌ — JDBC Session ✅

## 다중 WAR (10.12)

모든 WAR 동일 SESSIONDB → **세션 공유**

## application.yml (10.13)

```yaml
spring.session.store-type: jdbc
spring.session.timeout: ...
```

설정: `tcf-om/application.yml` — `spring.session.store-type: jdbc`, `spring.session.timeout: 60m`

## 중복 로그인 (10.15)

OM 정책 — 기존 세션 종료 또는 신규 거부 (`OM.Session.*`)

## 세션 오류 (10.17)

- 세션 없음 / 만료
- Header user ≠ session user → E-TCF-SES-*

## OM 운영 (10.18)

- `OmSessionHandler` — 조회·강제 종료
- `OM.Session.inquiry` 등

## 코드베이스

- `tcf-om/.../OmAuthHandler`, `OmSessionHandler`
- `tcf-gateway/.../UserSessionService.java`
- `zdoc/세션관리.md`, `zdoc/로그인.md`

## 운영 체크리스트 (10.19)

- [ ] SESSIONDB HA  
- [ ] Idle/Absolute 정책  
- [ ] Header-세션 불일치 알람  
- [ ] DR 시 세션 유지 검증  

## 이전 · 다음

← [9장 Gateway](./09-Gateway라우팅.md) · [11장 JWT/SSO](./11-JWT-SSO인증.md) →
