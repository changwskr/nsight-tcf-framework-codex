# A07 — 런타임·거래흐름

**선행:** [A06-목표-논리-애플리케이션.md](./A06-목표-논리-애플리케이션.md)  
**다음:** [A08-모듈-WAR-배포.md](./A08-모듈-WAR-배포.md)  
**결과 파일:** [`결과/A07-런타임-거래흐름.md`](./결과/A07-런타임-거래흐름.md)  
**구 대응:** M08  
**실소스:** `tcf-web/.../OnlineTransactionController.java`, `tcf-core/.../TCF.java`, `STF`, `ETF`, `OnlineTransactionTimeoutExecutor`, `TransactionDispatcher`

## 이 단계에서 얻는 것

정상·오류·Timeout·장애 End-to-End (실클래스명)

## 지금 이 질문을 붙여 넣으세요

```text
A-MASTER를 적용한다. 원장·tcf-web OnlineTransactionController·tcf-core TCF를 인용한다. 질문 1개씩.
단계 끝이면 결과/A07-런타임-거래흐름.md 와 원장 갱신 후
「다음 단계로 갈까요?」만 물어라.

온라인 정상 경로 Baseline으로 무엇을 고정할까요?

1. POST /online 또는 /{businessCode}/online
   → OnlineTransactionController → TCF → STF
   → OnlineTransactionTimeoutExecutor → TransactionDispatcher
   → TransactionHandler → Facade → Service → Rule/Dao → Mapper → ETF
   (권장·실소스)
2. 업무 WAR에 거래용 REST Controller 허용 (비권장·ADR·방법론 충돌)
3. 소스 확인 후 FQCN까지 적어 추천

권장: 1. `TimeoutExecutor`/`BTF` 등 없는 이름은 쓰지 마라.
```

## 추가 확인 (질문 1개씩)

업무오류 / 시스템오류 / Timeout / 중복(멱등) / 부분장애 / ETF 분기

## Gate

- 성공만 쓰지 않고 실패 흐름 포함
- 실클래스명 사용

## 결과 저장

`결과/A07-런타임-거래흐름.md` + 원장.
