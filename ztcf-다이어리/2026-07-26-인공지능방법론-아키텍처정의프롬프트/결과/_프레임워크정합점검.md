# 아키텍처 프롬프트 ↔ NSIGHT-TCF-FRAMEWORK 정합 점검

| 구분 | 내용 |
| --- | --- |
| 점검일 | 2026-07-26 |
| SoT | Branch `develop` 소스 + 방법론 `00-NSIGHT-TCF-AI-LLM-SI-개발방법론.md` |
| 대상 | A-MASTER, A00~A18, README, 원본 md |

## 불일치 → 조치

| # | 발견 | 조치 |
| --- | --- | --- |
| 1 | `TimeoutExecutor` 약칭 | 실클래스 `OnlineTransactionTimeoutExecutor`로 교정 |
| 2 | “Controller 금지” 모호 | **공통** `OnlineTransactionController`는 존재. 금지는 **업무 WAR의 거래용 Controller** |
| 3 | “6계층”=패키지 오해 | 논리 6단 = handler→…→mapper. 디스크는 `entry`/`application`/`persistence` (+eb는 client·config 등) |
| 4 | 포트 SoT | `tcf-ui` `BusinessModuleDefinitions` (AV 8101, LN 8103). `tcf-uj` 목록은 AV/LN 없을 수 있음 |
| 5 | Context Path | bootRun 로컬 `context-path: /` 흔함. Tomcat `/{biz}`는 배포 목표 — As-Is/To-Be 분리 |
| 6 | AV/LN | settings·tcf-ui에 존재. zarchitecture 구 WAR맵·ztomcat에 없을 수 있음 → Drift 후보 |
| 7 | 온라인 경로 | `POST /online` 및 `POST /{businessCode}/online` 둘 다 실존 |
| 8 | 시범 앵커 | `av-service`/`AV.Sample.inquiry`, `ln-service` 조회 시범을 프롬프트에 명시 |

## 유지 (정합 OK)

- SoT=현재 Branch, AA 승인, A16 Gate 전 최종생성 금지  
- 제외: 배치·파일·Gateway 신규(명시 확대 전)  
- 자동 Gate 상한=구조/명명, POST·배포=수동  
- Service→Mapper 직호출·타 WAR DAO 금지  
- Handler→Facade→Service→Rule→Dao→Mapper (av/ln)  
