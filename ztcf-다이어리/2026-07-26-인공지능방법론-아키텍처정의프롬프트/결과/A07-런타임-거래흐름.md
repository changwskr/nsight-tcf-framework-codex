# A07 — 런타임·거래흐름 (확정)

| 구분 | 내용 |
| --- | --- |
| 단계 | A07 |
| 사용자 답변 | 정상경로=1 / 실패흐름=1 |
| 확정사항 | 아래 절 |
| 가정·미확정 | 멱등 세부 정책 |
| 설계 영향 | A10~A12 Timeout·장애와 연결 |
| 원장 반영 | `_확정정보원장.md` |
| 다음 단계 | A08 — 모듈·WAR·배포 |

---

## 정상 경로 `[실제 소스 확인]`

```text
POST /online | /{businessCode}/online
  → OnlineTransactionController
  → TCF → STF
  → OnlineTransactionTimeoutExecutor
  → TransactionDispatcher
  → TransactionHandler → Facade → Service → Rule/Dao → Mapper
  → ETF
```

## 실패·Timeout

| 유형 | 처리 |
| --- | --- |
| 업무 오류 | → ETF businessFail |
| 시스템 오류 | → ETF systemError |
| Timeout | OnlineTransactionTimeoutExecutor |
| 멱등/중복 | 프레임 설정 · 세부 `[확인 필요]` |
| 부분장애 | WAR/DB 격리 · Runbook→A12 |

## Gate

- [x] 실패 흐름 포함
- [x] 실클래스명
