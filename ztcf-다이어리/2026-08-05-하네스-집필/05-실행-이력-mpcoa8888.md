# 05. 실행 이력 — mpcoa8888 안정화 (2026-08-05)

## 1. 요청 맥락

- Claude `/harness CRUD` 계열 요청에서 **옵션 1** 선택  
  → 신규 `mpcoa7777`이 아니라 **기존 `mpcoa8888` 안정화**
- 하네스: `tcf-harness-exe-pdmp`
- 대상: `pdmp-service`

## 2. Phase 위치

```text
tcf-harness-exe-pdmp/phases/1-mpcoa8888-stabilize/
  index.json
  step0.md … step3.md
  security-review.md
  qa-report.md
```

## 3. 문제 요약

| 증상 | 원인 |
|------|------|
| 기동/`test` 실패 | `ConflictingBeanDefinitionException` — `SecurityConfig` 이중 정의 (`nhnis.mp.config` vs `nhnis.fw.commons.configuration`) |
| TCF 트래픽 간섭 | legacy `DefaultFilter` / `WebConfiguration` / ResponseBodyAdvice / interceptor |
| JWT 기동 위험 | 빈 시크릿으로 `JwtProvider` 실패 가능 |

## 4. 조치 요약 (옵션1)

레거시 commons를 **기본 OFF** 조건으로 격리:

- `nhnis.fw.commons.security.enabled`
- `nhnis.fw.commons.filter.enabled`
- `nhnis.fw.commons.legacy-web.enabled`

로컬 JWT 시크릿 기본값 (`application.yml`):

```text
${PDMP_JWT_SECRET:pdmp-local-dev-secret-key-change-me-32b}
```

동일 패턴을 `pdmp-fw`에도 미러링 (라이브러리 쪽이 스캔될 때 대비).

## 5. 검증 결과 (요약)

- `SecurityConfigTest`, `mpcoa8888` 관련 테스트 GREEN
- `gradlew test` / `gradlew war` SUCCESS
- 관련 커밋 예: `8c2c086` — *Stabilize mpcoa8888 by isolating legacy commons from TCF*

상세 명령·로그는 phase의 `qa-report.md` / `security-review.md`를 SoT로 본다.

## 6. 명시적 미완 / 후속

| 항목 | 상태 |
|------|------|
| `pdmp-service` → `pdmp-fw` 의존 전환 + 중복 `nhnis.fw` 제거 | 미착수 |
| 신규 CRUD `mpcoa7777` | 미착수 (식별자·테이블·삭제정책 필요) |
| 운영 JWT (`jwt.enabled=true` + 발급) | 미착수 |
| Oracle 검증 | 미검증 (H2만) |

## 7. 교훈 (하네스 관점)

1. **안정화와 신규 CRUD를 옵션으로 분리**하니 범위 폭주가 줄었다.
2. 레거시 FW와 TCF가 한 앱에 공존하면 **Bean/Filter 충돌**이 1순위 장애다 → “default-off conditional”이 유효한 격리 패턴.
3. 증적은 `phases/`에 남기고, 다이어리(본 폴더)는 **지도·선택·이력 요약**만 담당한다.
