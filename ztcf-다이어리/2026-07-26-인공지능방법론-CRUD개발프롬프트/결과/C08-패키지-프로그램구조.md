# C08 — 패키지·프로그램 구조 (확정)

| 구분 | 내용 |
| --- | --- |
| 단계 | C08 |
| 사용자 답변 | 계층=1 av동일 / Handler=1 도메인1개 / 프로그램목록=1 |
| 확정사항 | 아래 절 |
| 가정·미확정 | Mapper XML 경로(C09) |
| 설계 영향 | ln-service 골격 전체. TX=@Transactional on Facade |
| 원장 반영 | `_확정정보원장.md` |
| 다음 단계 | C09 — SQL·Mapper |

---

## 베이스·계층

| 항목 | 값 |
| --- | --- |
| BASE | `com.nh.nsight.marketing.ln` |
| 기준 | av-service 실구조 |

```text
entry/handler → entry/facade → application/service · rule
                              → persistence/dao → mapper
```

## 프로그램 목록 `[설계 예시]`

| 계층 | 클래스 |
| --- | --- |
| Bootstrap | `NsightLnServiceApplication` |
| Handler | `LnCustomerContactHandler` |
| Facade | `LnCustomerContactFacade` |
| Service | `LnCustomerContactService` |
| Rule | `LnCustomerContactRule` |
| Dao | `LnCustomerContactDao` |
| Mapper | `LnCustomerContactMapper` |

Handler `serviceIds`: `selectList`, `selectDetail`.

## TX·금지

| 항목 | 내용 |
| --- | --- |
| TX 경계 | Facade (`readOnly=true`, timeout 예: 5) |
| 금지 | Handler→Mapper, Service→Mapper 직호출, Rule→DAO, 타 WAR Java, 업무 Controller |

## Gate

- [x] 계층 책임 분리
- [x] TX 경계 = Facade
- [x] 패키지↔업무코드·도메인
