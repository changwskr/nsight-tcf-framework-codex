# C15 — 소스·설정·문서 생성 (확정)

| 구분 | 내용 |
| --- | --- |
| 단계 | C15 |
| 사용자 답변 | 파일목록=1 승인 |
| 확정사항 | ln-service·tcf-ui·등록 생성 완료. `compileJava` 성공 |
| 가정·미확정 | OM 실테이블 INSERT(AA), 공식 채번, C16 기동·POST 검증 |
| 설계 영향 | bootRun 8103 가능. CUD·Gateway 미생성 |
| 원장 반영 | `_확정정보원장.md` |
| 다음 단계 | C16 — 빌드·기동·거래검증 |

---

## 생성 요약

| 영역 | 내용 |
| --- | --- |
| 모듈 | `ln-service` (ln.war, port 8103) |
| 거래 | `selectList` / `selectDetail` |
| 계층 | Handler→Facade→Service→Rule→Dao→Mapper |
| UI | `static/ln/contact-list.html` + `_shared/ln-admin.js` |
| OM | `resources/om/ln-om-seed-draft.sql` 초안만 |
| 등록 | settings / businessModules / BusinessModuleDefinitions LN |

## 추가 생성(목록 의존)

| 파일 | 사유 |
| --- | --- |
| `tcf-ui/.../_shared/ln-admin.js` | contact-list 릴레이 필수 |
| `tcf-ui/.../static/index.html` LN 링크 | 허브 진입 |

## Gate

- [x] 파일목록 사용자 확인
- [x] 실제 API·av 패턴 복제
- [x] 보호영역 덮지 않음 (신규만)
- [x] `:ln-service:compileJava` 성공
