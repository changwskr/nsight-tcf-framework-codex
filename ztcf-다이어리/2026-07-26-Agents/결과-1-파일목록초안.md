# 결과-1 파일목록 초안 (CRUD Codegen)

입력: [`../2026-07-26-인공지능방법론-CRUD개발프롬프트/결과-1/`](../2026-07-26-인공지능방법론-CRUD개발프롬프트/결과-1/)  
기준일: 2026-07-26  
상태: **갭 모드 승인·실행 완료**

## 선택 결과

- 사용자 선택: `1 — 갭 모드`
- 대상: 기존 `av-service` CustomerContact 구현 보호
- 보완: Rule 단위 테스트, TCF UI AV 화면 링크, 결과 마크다운 AV 정합
- 검증: `:av-service:test`, `:tcf-ui:processResources` 성공

## 1. 원장 요약

| 항목 | 값 |
| --- | --- |
| C14 Gate | `CONDITIONAL` (OM 실등록·포트/거래코드 공식채번 Gap) |
| c00 | 신규 모듈 · base `ln-service` · bootRun |
| c01 | BC=`AV` · Domain=`CustomerContact` |
| c06 (파일) | `LN.CustomerContact.selectList/Detail` |
| c06 (원장) | `AV.CustomerContact.selectList/Detail` |
| c08 | Handler=`LnCustomerContactHandler` |
| c04 | `LN_CUSTOMER_CONTACT` / PK `CONTACT_ID` |
| c15 | 이미 구현됨 — `ln-service` CustomerContact 경로 기록 |

## 2. 정합 충돌 (생성 전 해소 필요)

| # | 충돌 | 선택지 |
| --- | --- | --- |
| A | BC: 원장 `AV` vs C06 파일 `LN` vs Handler `Ln*` | (1) 전부 `AV`/`Av*`/`av-service` (2) 전부 `LN`/`Ln*`/`ln-service` |
| B | c00 “신규 모듈” vs 저장소에 `av-service`·`ln-service` CustomerContact **이미 존재** | (1) 갭만 보완 (2) 지정 모듈에 재생성(위험) |
| C | 테이블명 `LN_*` vs BC `AV` | 테이블을 BC에 맞출지, 소유 WAR만 맞출지 확정 |

**권고:** 실제 소스가 있는 `av-service`의 `AvCustomerContact*`를 대상으로 **갭 대조**하고, serviceId는 `AV.CustomerContact.*`로 통일. Handler는 `AvCustomerContactHandler` (원장 `Ln*`는 오기정정).

충돌은 AV 기준으로 해소했다. 기존 업무 구현은 재생성하지 않았다.

## 3. 기존 구현 대조 (`av-service`)

이미 존재하는 경로 (재생성 기본 **스킵**):

| 경로 | 권장 |
| --- | --- |
| `av-service/.../entry/handler/AvCustomerContactHandler.java` | 스킵(보호) |
| `av-service/.../entry/facade/AvCustomerContactFacade.java` | 스킵 |
| `av-service/.../application/service/AvCustomerContactService.java` | 스킵 |
| `av-service/.../application/rule/AvCustomerContactRule.java` | 스킵 |
| `av-service/.../persistence/dao/AvCustomerContactDao.java` | 스킵 |
| `av-service/.../persistence/mapper/AvCustomerContactMapper.java` | 스킵 |
| `av-service/.../resources/mapper/av/AvCustomerContactMapper.xml` | 스킵 |
| `av-service/.../application/dto/customercontact/*` | 스킵 |
| `av-service/.../persistence/dto/customercontact/CustomerContactRow.java` | 스킵 |

## 4. 기준 복제 소스 (`ln-service`)

패턴 참고용 (복사 원본, 직접 덮지 않음):

- `ln-service/.../LnCustomerContactHandler.java` 및 동일 도메인 Facade·Service·Rule·Dao·Mapper·DTO

## 5. 생성·보완 후보 (갭)

원장 `c15`가 “이미 구현”이므로 **전체 생성 대신** 아래만 후보로 둔다. 실제 갭은 코드 Diff 후 확정.

| 경로 | 신규/수정 | 근거 |
| --- | --- | --- |
| `av-service` 테스트 (`*CustomerContact*Test`) | 신규(없을 때) | C12 |
| `tcf-ui` sample-requests / 화면 링크 (`/av/contact-list.html`) | 수정(없을 때) | C03 |
| OM Catalog·거래코드 등록 | 스킵 또는 Gap 유지 | C13·C14 CONDITIONAL |
| `결과-1/C15-소스-설정-문서생성.md` · `_확정정보원장.md` | 수정 | C15 — 구현 경로·정합 정정 기록 |

신규 모듈을 정말로 다시 깐다면(비권장) C00 기준 전체 트리가 필요하나, 현재 저장소 상태와 충돌한다.

## 6. 승인 결과

- [x] **갭 모드** — `av-service` 기존 구현 유지
- [x] serviceId/원장을 `AV`로 정합
- [x] 테스트·UI·C15/C16 문서 보완
